/*! plotpolish v0.2.0 | (c) 2026 Larry Engelhardt and plotpolish contributors | SPDX-License-Identifier: BSD-3-Clause | https://github.com/lengelhardt/plotpolish */
(function(exports) {
  "use strict";
  const TOOL_NAME = "plotpolish";
  const VERSION = "0.2.0";
  const ELEMENT_TAG = "plotpolish-panel";
  const EVENT_PREFIX = TOOL_NAME;
  const FENCE_START = `# --- plot style (generated by ${TOOL_NAME}; edit above, not here) ---`;
  const FENCE_END = "# --- end plot style ---";
  const RESULT_VARIABLE = `__${TOOL_NAME}_result__`;
  const groups = [{ "id": "look", "label": "Look", "help": "The overall style: preset and colors." }, { "id": "text", "label": "Text", "help": "Sizes and font for every piece of text." }, { "id": "lines", "label": "Lines", "help": "Plotted lines and markers." }, { "id": "axes", "label": "Axes", "help": "Grid, the box around the plot, tick marks.", "subgroups": [{ "id": "grid", "label": "Grid" }, { "id": "box", "label": "Box and axes lines" }, { "id": "ticks", "label": "Tick marks" }] }, { "id": "legend", "label": "Legend", "help": "Where the legend sits and how it is framed.", "hideWhen": "no-legend" }, { "id": "save", "label": "Save", "help": "Applies when the figure is saved, not on screen." }];
  const controls = [{ "id": "style", "group": "look", "tier": "primary", "label": "Style preset", "type": "style", "keys": [], "default": "default", "category": "rerun", "help": "One of matplotlib's style sheets (plt.style.available). Sets many defaults at once; the other controls override it. Applies on the next run." }, { "id": "prop_cycle", "group": "look", "tier": "primary", "label": "Colors", "type": "colorcycle", "keys": ["axes.prop_cycle"], "category": "live", "help": "The color given to the 1st, 2nd, … line (axes.prop_cycle). Applies to the lines already drawn and to new ones.", "default": ["#1f77b4", "#ff7f0e", "#2ca02c", "#d62728", "#9467bd", "#8c564b", "#e377c2", "#7f7f7f", "#bcbd22", "#17becf"], "presets": [{ "id": "tab10", "label": "matplotlib default", "colors": ["#1f77b4", "#ff7f0e", "#2ca02c", "#d62728", "#9467bd", "#8c564b", "#e377c2", "#7f7f7f", "#bcbd22", "#17becf"] }, { "id": "okabe-ito", "label": "Colorblind-safe (Okabe–Ito)", "colors": ["#E69F00", "#56B4E9", "#009E73", "#F0E442", "#0072B2", "#D55E00", "#CC79A7", "#000000"] }, { "id": "tol-bright", "label": "Colorblind-safe (Tol bright)", "colors": ["#4477AA", "#EE6677", "#228833", "#CCBB44", "#66CCEE", "#AA3377", "#BBBBBB"] }] }, { "id": "font_size", "group": "text", "tier": "primary", "label": "Text size", "type": "number", "keys": ["font.size"], "default": 10, "category": "live", "min": 6, "max": 24, "step": 0.5, "help": "Base size in points (font.size). Title, labels and legend scale from it unless set below." }, { "id": "autolayout", "group": "text", "tier": "primary", "label": "Fit labels in figure", "type": "bool", "keys": ["figure.autolayout"], "default": false, "panelDefault": true, "category": "live", "help": "Re-lays out the figure on every draw so axis labels and titles stay inside the edges (figure.autolayout, matplotlib's tight_layout). On by default, because enlarging text is the first thing most people do and it clips without this." }, { "id": "title_size", "group": "text", "tier": "more", "label": "Title size", "type": "fontsize", "keys": ["axes.titlesize"], "default": "large", "category": "live", "help": "axes.titlesize" }, { "id": "label_size", "group": "text", "tier": "more", "label": "Axis label size", "type": "fontsize", "keys": ["axes.labelsize"], "default": "medium", "category": "live", "help": "axes.labelsize" }, { "id": "tick_label_size", "group": "text", "tier": "more", "label": "Tick label size", "type": "fontsize", "keys": ["xtick.labelsize", "ytick.labelsize"], "default": "medium", "category": "live", "help": "xtick.labelsize and ytick.labelsize" }, { "id": "legend_fontsize", "group": "text", "tier": "more", "label": "Legend text size", "type": "fontsize", "keys": ["legend.fontsize"], "default": "medium", "category": "live", "help": "legend.fontsize" }, { "id": "font_family", "group": "text", "tier": "more", "label": "Font", "type": "enum", "keys": ["font.family"], "default": "sans-serif", "category": "live", "help": "font.family. Applied to the text already on the figure and to new text.", "options": [{ "value": "sans-serif", "label": "Sans" }, { "value": "serif", "label": "Serif" }, { "value": "monospace", "label": "Mono" }] }, { "id": "line_cycle", "group": "lines", "tier": "primary", "label": "Per line", "type": "linecycle", "keys": ["axes.prop_cycle"], "category": "live", "default": ["#1f77b4", "#ff7f0e", "#2ca02c", "#d62728", "#9467bd", "#8c564b", "#e377c2", "#7f7f7f", "#bcbd22", "#17becf"], "help": "Color, width and style for the 1st, 2nd, … line in the order they are drawn (axes.prop_cycle). A line whose code sets its own color or width keeps it.", "minLines": 2, "maxLines": 8 }, { "id": "linewidth", "group": "lines", "tier": "primary", "label": "Line width (all)", "type": "number", "keys": ["lines.linewidth"], "default": 1.5, "category": "live", "min": 0.5, "max": 8, "step": 0.25, "help": "lines.linewidth, in points" }, { "id": "linestyle", "group": "lines", "tier": "more", "label": "Line style (all)", "type": "enum", "keys": ["lines.linestyle"], "default": "-", "category": "live", "help": "lines.linestyle", "options": [{ "value": "-", "label": "Solid" }, { "value": "--", "label": "Dashed" }, { "value": "-.", "label": "Dash-dot" }, { "value": ":", "label": "Dotted" }] }, { "id": "marker", "group": "lines", "tier": "more", "label": "Marker style", "type": "enum", "keys": ["lines.marker"], "default": "None", "category": "live", "help": "lines.marker: a symbol at every data point. 'None' draws lines only.", "options": [{ "value": "None", "label": "None" }, { "value": "o", "label": "● Circle" }, { "value": "s", "label": "■ Square" }, { "value": "^", "label": "▲ Triangle" }, { "value": "D", "label": "◆ Diamond" }, { "value": "x", "label": "✕ X" }, { "value": "+", "label": "+ Plus" }, { "value": "*", "label": "★ Star" }, { "value": ".", "label": "· Point" }] }, { "id": "markersize", "group": "lines", "tier": "more", "label": "Marker size", "type": "number", "keys": ["lines.markersize"], "default": 6, "category": "live", "min": 1, "max": 12, "step": 0.5, "help": "lines.markersize, in points" }, { "id": "grid", "group": "axes", "subgroup": "grid", "tier": "primary", "label": "Grid", "type": "bool", "keys": ["axes.grid"], "default": false, "category": "live", "help": "axes.grid" }, { "id": "grid_alpha", "group": "axes", "subgroup": "grid", "tier": "more", "label": "Grid opacity", "type": "number", "keys": ["grid.alpha"], "default": 1, "category": "live", "min": 0, "max": 1, "step": 0.05, "help": "grid.alpha" }, { "id": "grid_linestyle", "group": "axes", "subgroup": "grid", "tier": "more", "label": "Grid line style", "type": "enum", "keys": ["grid.linestyle"], "default": "-", "category": "live", "help": "grid.linestyle", "options": [{ "value": "-", "label": "Solid" }, { "value": "--", "label": "Dashed" }, { "value": "-.", "label": "Dash-dot" }, { "value": ":", "label": "Dotted" }] }, { "id": "box", "group": "axes", "subgroup": "box", "tier": "primary", "label": "Box around the plot", "type": "bool", "keys": ["axes.spines.top", "axes.spines.right"], "default": true, "category": "live", "help": "Off hides the top and right edges (axes.spines.top and axes.spines.right)." }, { "id": "axes_linewidth", "group": "axes", "subgroup": "box", "tier": "more", "label": "Axes line width", "type": "number", "keys": ["axes.linewidth"], "default": 0.8, "category": "live", "min": 0.25, "max": 4, "step": 0.05, "help": "axes.linewidth" }, { "id": "tick_direction", "group": "axes", "subgroup": "ticks", "tier": "more", "label": "Tick marks point", "type": "enum", "keys": ["xtick.direction", "ytick.direction"], "default": "out", "category": "live", "help": "xtick.direction and ytick.direction", "options": [{ "value": "out", "label": "Out" }, { "value": "in", "label": "In" }, { "value": "inout", "label": "Both" }] }, { "id": "minor_ticks", "group": "axes", "subgroup": "ticks", "tier": "more", "label": "Minor tick marks", "type": "bool", "keys": ["xtick.minor.visible", "ytick.minor.visible"], "default": false, "category": "live", "help": "xtick.minor.visible and ytick.minor.visible" }, { "id": "minor_grid", "group": "axes", "tier": "more", "label": "Minor grid lines", "type": "bool", "keys": ["axes.grid.which"], "default": "major", "onValue": "both", "offValue": "major", "category": "live", "help": "Draw grid lines at the minor ticks as well as the major ones (axes.grid.which). Needs Minor ticks on: matplotlib only draws a minor grid line where a minor tick exists." }, { "id": "legend_loc", "group": "legend", "tier": "primary", "label": "Position", "type": "legendloc", "keys": ["legend.loc"], "default": "best", "category": "live", "help": "legend.loc. Drag x and y to place the legend's lower-left corner as fractions of the axes; 'Snap to' picks one of matplotlib's named spots (Best lets matplotlib choose).", "options": [{ "value": "best", "label": "Best" }, { "value": "upper right", "label": "Upper right" }, { "value": "upper left", "label": "Upper left" }, { "value": "lower left", "label": "Lower left" }, { "value": "lower right", "label": "Lower right" }, { "value": "right", "label": "Right" }, { "value": "center left", "label": "Center left" }, { "value": "center right", "label": "Center right" }, { "value": "lower center", "label": "Lower center" }, { "value": "upper center", "label": "Upper center" }, { "value": "center", "label": "Center" }] }, { "id": "legend_frameon", "group": "legend", "tier": "primary", "label": "Frame", "type": "bool", "keys": ["legend.frameon"], "default": true, "category": "live", "help": "legend.frameon" }, { "id": "legend_framealpha", "group": "legend", "tier": "more", "label": "Frame opacity", "type": "number", "keys": ["legend.framealpha"], "default": 0.8, "category": "live", "min": 0, "max": 1, "step": 0.05, "help": "legend.framealpha" }, { "id": "savefig_dpi", "group": "save", "tier": "primary", "label": "Resolution (dpi)", "type": "dpi", "keys": ["savefig.dpi"], "default": "figure", "category": "save", "min": 36, "max": 1200, "step": 1, "help": "savefig.dpi for plt.savefig(). plotpolish sets 300 as soon as you change anything; matplotlib's own default is the figure's screen dpi.", "panelDefault": 300 }, { "id": "savefig_transparent", "group": "save", "tier": "primary", "label": "Transparent background", "type": "bool", "keys": ["savefig.transparent"], "default": false, "category": "save", "help": "savefig.transparent" }, { "id": "savefig_bbox", "group": "save", "tier": "primary", "label": "Crop to content", "type": "enum", "keys": ["savefig.bbox"], "default": "standard", "category": "save", "help": "savefig.bbox", "options": [{ "value": "standard", "label": "No" }, { "value": "tight", "label": "Yes" }] }, { "id": "copy_code", "group": "save", "tier": "primary", "label": "Copy code", "type": "copycode", "keys": [], "default": false, "category": "save", "help": "Copy the generated block to the clipboard, to paste into a script that does not have the panel." }];
  const raw = {
    groups,
    controls
  };
  function isPropCycle(v) {
    return typeof v === "object" && v !== null && !Array.isArray(v) && Array.isArray(v.color);
  }
  const schema = raw;
  const GROUPS = schema.groups;
  const CONTROLS = schema.controls;
  const CONTROL_BY_ID = new Map(
    CONTROLS.map((c) => [c.id, c])
  );
  const CONTROL_FOR_KEY = new Map(
    CONTROLS.flatMap((c) => c.keys.map((k) => [k, c]))
  );
  const RC_KEYS = [...new Set(CONTROLS.flatMap((c) => c.keys))];
  function categoryOf(key) {
    return CONTROL_FOR_KEY.get(key)?.category;
  }
  function controlsInGroup(groupId, tier) {
    return CONTROLS.filter((c) => c.group === groupId && (tier === void 0 || c.tier === tier));
  }
  const GROUP_BY_ID = new Map(GROUPS.map((g) => [g.id, g]));
  const FONT_SCALINGS = {
    "xx-small": 0.579,
    "x-small": 0.694,
    small: 0.833,
    medium: 1,
    large: 1.2,
    "x-large": 1.44,
    "xx-large": 1.728,
    larger: 1.2,
    smaller: 0.833
  };
  const RELATIVE_SIZES = [
    "xx-small",
    "x-small",
    "small",
    "medium",
    "large",
    "x-large",
    "xx-large"
  ];
  function resolveFontSize(value, base) {
    if (typeof value === "number") return value;
    if (typeof value === "string") {
      const scale = FONT_SCALINGS[value];
      if (scale !== void 0) return base * scale;
      const n = Number(value);
      if (Number.isFinite(n)) return n;
    }
    return base;
  }
  function rcEqual(a, b) {
    if (a === void 0 || b === void 0) return a === b;
    if (isPropCycle(a) || isPropCycle(b)) {
      const pa = isPropCycle(a) ? a : Array.isArray(a) ? { color: a } : null;
      const pb = isPropCycle(b) ? b : Array.isArray(b) ? { color: b } : null;
      if (!pa || !pb) return false;
      return rcEqual(pa.color, pb.color) && rcEqual(pa.linewidth, pb.linewidth) && rcEqual(pa.linestyle, pb.linestyle);
    }
    if (Array.isArray(a) || Array.isArray(b)) {
      if (!Array.isArray(a) || !Array.isArray(b) || a.length !== b.length) return false;
      return a.every((v, i) => rcEqual(v, b[i]));
    }
    if (typeof a === "number" && typeof b === "number") {
      return Math.abs(a - b) <= 1e-9 * Math.max(1, Math.abs(a), Math.abs(b));
    }
    return a === b;
  }
  function isPyCycler(v) {
    return typeof v === "object" && v !== null && !Array.isArray(v) && "cycler" in v;
  }
  function isPyTuple(v) {
    return typeof v === "object" && v !== null && !Array.isArray(v) && "tuple" in v;
  }
  class PyLitError extends Error {
    constructor(message, offset) {
      super(`${message} (at offset ${offset})`);
      this.name = "PyLitError";
      this.offset = offset;
    }
  }
  const PUNCT = /* @__PURE__ */ new Set(["{", "}", "[", "]", "(", ")", ":", ",", "="]);
  const NUM_RE = /^-?(?:\d+\.?\d*|\.\d+)(?:[eE][+-]?\d+)?/;
  const NAME_RE = /^[A-Za-z_][A-Za-z0-9_.]*/;
  class Lexer {
    constructor(src) {
      this.src = src;
      this.i = 0;
      this.buffered = null;
    }
    /** Offset just past the last token handed out. */
    get offset() {
      return this.buffered ? this.buffered.pos : this.i;
    }
    peek() {
      if (!this.buffered) this.buffered = this.read();
      return this.buffered;
    }
    next() {
      const t = this.peek();
      this.buffered = null;
      return t;
    }
    read() {
      const src = this.src;
      while (this.i < src.length) {
        const ch2 = src[this.i];
        if (ch2 === " " || ch2 === "	" || ch2 === "\n" || ch2 === "\r") {
          this.i++;
          continue;
        }
        if (ch2 === "#") {
          while (this.i < src.length && src[this.i] !== "\n") this.i++;
          continue;
        }
        break;
      }
      if (this.i >= src.length) return { kind: "eof", pos: src.length };
      const pos = this.i;
      const ch = src[pos];
      if (PUNCT.has(ch)) {
        this.i++;
        return { kind: "punct", value: ch, pos };
      }
      if (ch === '"' || ch === "'") {
        const { value, end } = readString(src, pos);
        this.i = end;
        return { kind: "str", value, pos };
      }
      const rest = src.slice(pos);
      const num = NUM_RE.exec(rest);
      if (num && (ch === "-" || ch === "." || ch >= "0" && ch <= "9")) {
        const n = Number(num[0]);
        if (!Number.isFinite(n)) throw new PyLitError(`Bad number ${num[0]}`, pos);
        this.i += num[0].length;
        return { kind: "num", value: n, pos };
      }
      const name = NAME_RE.exec(rest);
      if (name) {
        this.i += name[0].length;
        return { kind: "name", value: name[0], pos };
      }
      throw new PyLitError(`Unexpected character ${JSON.stringify(ch)}`, pos);
    }
  }
  const SIMPLE_ESCAPES = {
    n: "\n",
    t: "	",
    r: "\r",
    "\\": "\\",
    '"': '"',
    "'": "'",
    "0": "\0",
    b: "\b",
    f: "\f",
    v: "\v"
  };
  function readString(src, start) {
    const quote = src[start];
    let i = start + 1;
    let value = "";
    while (i < src.length) {
      const ch = src[i];
      if (ch === quote) return { value, end: i + 1 };
      if (ch === "\n") throw new PyLitError("Unterminated string", start);
      if (ch === "\\") {
        const esc = src[i + 1];
        if (esc === void 0) throw new PyLitError("Unterminated escape", i);
        if (esc === "u" || esc === "x") {
          const len = esc === "u" ? 4 : 2;
          const hex = src.slice(i + 2, i + 2 + len);
          if (!new RegExp(`^[0-9a-fA-F]{${len}}$`).test(hex)) throw new PyLitError("Bad escape", i);
          value += String.fromCharCode(parseInt(hex, 16));
          i += 2 + len;
          continue;
        }
        const simple = SIMPLE_ESCAPES[esc];
        if (simple === void 0) throw new PyLitError(`Unsupported escape \\${esc}`, i);
        value += simple;
        i += 2;
        continue;
      }
      value += ch;
      i++;
    }
    throw new PyLitError("Unterminated string", start);
  }
  class Parser {
    constructor(lexer) {
      this.lexer = lexer;
    }
    peek() {
      return this.lexer.peek();
    }
    next() {
      return this.lexer.next();
    }
    expectPunct(p) {
      const t = this.next();
      if (t.kind !== "punct" || t.value !== p) {
        throw new PyLitError(`Expected ${JSON.stringify(p)}`, t.pos);
      }
    }
    isPunct(p) {
      const t = this.peek();
      return t.kind === "punct" && t.value === p;
    }
    dict() {
      this.expectPunct("{");
      const out = {};
      while (!this.isPunct("}")) {
        const k = this.next();
        if (k.kind !== "str") throw new PyLitError("Dict keys must be strings", k.pos);
        if (k.value in out) throw new PyLitError(`Duplicate key ${JSON.stringify(k.value)}`, k.pos);
        this.expectPunct(":");
        out[k.value] = this.value();
        if (this.isPunct(",")) {
          this.next();
        } else if (!this.isPunct("}")) {
          throw new PyLitError('Expected "," or "}"', this.peek().pos);
        }
      }
      this.expectPunct("}");
      return out;
    }
    value() {
      const t = this.peek();
      if (t.kind === "str" || t.kind === "num") {
        this.next();
        return t.value;
      }
      if (t.kind === "name") {
        if (t.value === "True" || t.value === "False") {
          this.next();
          return t.value === "True";
        }
        if (t.value === "mpl.cycler") return this.cycler();
        throw new PyLitError(`Unsupported name ${t.value}`, t.pos);
      }
      if (t.kind === "punct" && t.value === "[") return this.list();
      if (t.kind === "punct" && t.value === "(") return this.tuple();
      throw new PyLitError("Expected a value", t.pos);
    }
    list() {
      this.expectPunct("[");
      const out = [];
      while (!this.isPunct("]")) {
        const t = this.next();
        if (t.kind === "str" || t.kind === "num") out.push(t.value);
        else if (t.kind === "name" && (t.value === "True" || t.value === "False")) out.push(t.value === "True");
        else throw new PyLitError("Lists may only hold strings and numbers", t.pos);
        if (this.isPunct(",")) this.next();
        else if (!this.isPunct("]")) throw new PyLitError('Expected "," or "]"', this.peek().pos);
      }
      this.expectPunct("]");
      return out;
    }
    tuple() {
      this.expectPunct("(");
      const out = [];
      while (!this.isPunct(")")) {
        const t = this.next();
        if (t.kind === "str" || t.kind === "num") out.push(t.value);
        else if (t.kind === "name" && (t.value === "True" || t.value === "False")) out.push(t.value === "True");
        else throw new PyLitError("Tuples may only hold strings and numbers", t.pos);
        if (this.isPunct(",")) this.next();
        else if (!this.isPunct(")")) throw new PyLitError('Expected "," or ")"', this.peek().pos);
      }
      this.expectPunct(")");
      return { tuple: out };
    }
    cycler() {
      this.next();
      this.expectPunct("(");
      const result = {};
      while (!this.isPunct(")")) {
        const kw = this.next();
        if (kw.kind !== "name") throw new PyLitError("Expected a keyword in mpl.cycler(...)", kw.pos);
        if (kw.value !== "color" && kw.value !== "linewidth" && kw.value !== "linestyle") {
          throw new PyLitError(`Unsupported mpl.cycler keyword ${JSON.stringify(kw.value)}`, kw.pos);
        }
        if (result[kw.value] !== void 0) throw new PyLitError(`Duplicate mpl.cycler keyword ${JSON.stringify(kw.value)}`, kw.pos);
        this.expectPunct("=");
        const list = this.list();
        if (kw.value === "linewidth") {
          if (!list.every((v) => typeof v === "number")) throw new PyLitError("mpl.cycler linewidth values must be numbers", kw.pos);
          result.linewidth = list;
        } else if (kw.value === "color") {
          if (!list.every((v) => typeof v === "string")) throw new PyLitError("mpl.cycler color values must be strings", kw.pos);
          result.color = list;
        } else {
          if (!list.every((v) => typeof v === "string")) throw new PyLitError("mpl.cycler linestyle values must be strings", kw.pos);
          result.linestyle = list;
        }
        if (this.isPunct(",")) this.next();
        else if (!this.isPunct(")")) throw new PyLitError('Expected "," or ")"', this.peek().pos);
      }
      this.expectPunct(")");
      return { cycler: result };
    }
  }
  function parsePyDict(src) {
    const lexer = new Lexer(src);
    const p = new Parser(lexer);
    const value = p.dict();
    return { value, end: lexer.offset };
  }
  function parsePyString(src) {
    const lexer = new Lexer(src);
    const t = lexer.next();
    if (t.kind !== "str") throw new PyLitError("Expected a string literal", t.pos);
    const after = lexer.next();
    if (after.kind !== "eof") throw new PyLitError("Unexpected text after the string", after.pos);
    return t.value;
  }
  function formatPyValue(value) {
    if (typeof value === "boolean") return value ? "True" : "False";
    if (typeof value === "number") {
      if (!Number.isFinite(value)) throw new PyLitError(`Cannot emit ${value}`, 0);
      return String(value);
    }
    if (typeof value === "string") return JSON.stringify(value);
    if (Array.isArray(value)) return `[${value.map(formatPyValue).join(", ")}]`;
    if (isPyCycler(value)) {
      const { color, linewidth, linestyle } = value.cycler;
      const parts = [];
      if (color) parts.push(`color=${formatPyValue(color)}`);
      if (linewidth) parts.push(`linewidth=${formatPyValue(linewidth)}`);
      if (linestyle) parts.push(`linestyle=${formatPyValue(linestyle)}`);
      return `mpl.cycler(${parts.join(", ")})`;
    }
    if (isPyTuple(value)) {
      if (value.tuple.length === 0) return "()";
      if (value.tuple.length === 1) return `(${formatPyValue(value.tuple[0])},)`;
      return `(${value.tuple.map(formatPyValue).join(", ")})`;
    }
    throw new PyLitError("Unsupported value", 0);
  }
  class FenceError extends Error {
    constructor(kind, message, line) {
      super(message);
      this.name = "FenceError";
      this.kind = kind;
      this.line = line;
    }
  }
  function defaultSettings() {
    return { style: "default", rc: {} };
  }
  function isDefaultSettings(s) {
    return (s.style === "default" || s.style === "") && Object.keys(s.rc).length === 0;
  }
  function detectEol(source) {
    return source.includes("\r\n") ? "\r\n" : "\n";
  }
  function splitLines(source) {
    return source.split(/\r?\n/);
  }
  function isStart(line) {
    return line.trimEnd() === FENCE_START;
  }
  function isEnd(line) {
    return line.trimEnd() === FENCE_END;
  }
  function findFence(source) {
    const lines = splitLines(source);
    let start = -1;
    let end = -1;
    lines.forEach((line, i) => {
      const t = line.trim();
      if (t === FENCE_START || t === FENCE_END) {
        if (line.trimEnd() !== t) {
          throw new FenceError("indented", `Fence marker on line ${i + 1} is indented; markers must start the line.`, i);
        }
      }
      if (isStart(line)) {
        if (start !== -1) throw new FenceError("multiple-start", `Second plot-style block starts on line ${i + 1}; there must be exactly one.`, i);
        start = i;
      } else if (isEnd(line)) {
        if (end !== -1) throw new FenceError("multiple-end", `Second "end plot style" marker on line ${i + 1}.`, i);
        end = i;
      }
    });
    if (start === -1 && end === -1) return null;
    if (start === -1) throw new FenceError("orphan-end", `"end plot style" marker on line ${end + 1} has no start.`, end);
    if (end === -1) throw new FenceError("unterminated", `Plot-style block starting on line ${start + 1} has no end marker.`, start);
    if (end < start) throw new FenceError("out-of-order", `"end plot style" marker (line ${end + 1}) comes before the start (line ${start + 1}).`, end);
    return { start, end };
  }
  function toPyValue(key, value) {
    if (key === "axes.prop_cycle") {
      if (Array.isArray(value)) {
        return { cycler: { color: value.map(String) } };
      }
      if (isPropCycle(value)) {
        const cycler = { color: value.color };
        if (value.linewidth) cycler.linewidth = value.linewidth;
        if (value.linestyle) cycler.linestyle = value.linestyle;
        return { cycler };
      }
    }
    if (key === "legend.loc" && Array.isArray(value)) {
      return { tuple: value };
    }
    return value;
  }
  function orderedKeys(rc) {
    const known = RC_KEYS.filter((k) => k in rc);
    const unknown = Object.keys(rc).filter((k) => !CONTROL_FOR_KEY.has(k));
    return [...known, ...unknown];
  }
  function generateBlock(settings) {
    if (isDefaultSettings(settings)) return null;
    const lines = [FENCE_START, "import matplotlib as mpl"];
    if (settings.style && settings.style !== "default") {
      lines.push(`mpl.style.use(${JSON.stringify(settings.style)})`);
    }
    const keys = orderedKeys(settings.rc);
    if (keys.length) {
      lines.push("mpl.rcParams.update({");
      for (const key of keys) {
        lines.push(`    ${JSON.stringify(key)}: ${formatPyValue(toPyValue(key, settings.rc[key]))},`);
      }
      lines.push("})");
    }
    lines.push(FENCE_END);
    return lines.join("\n");
  }
  const STYLE_RE = /^\s*mpl\.style\.use\((.*)\)\s*$/;
  function fromPyValue(key, value, line) {
    if (isPyCycler(value)) {
      if (key !== "axes.prop_cycle") throw new FenceError("malformed", `mpl.cycler is only valid for "axes.prop_cycle" (line ${line + 1}).`, line);
      const { color, linewidth, linestyle } = value.cycler;
      if (!color) {
        throw new FenceError("malformed", `mpl.cycler for "axes.prop_cycle" is missing "color", which is required (line ${line + 1}).`, line);
      }
      if (linewidth === void 0 && linestyle === void 0) return color;
      const result = { color };
      if (linewidth !== void 0) result.linewidth = linewidth;
      if (linestyle !== void 0) result.linestyle = linestyle;
      return result;
    }
    if (isPyTuple(value)) {
      if (value.tuple.every((v) => typeof v === "number")) return value.tuple;
      if (value.tuple.every((v) => typeof v === "string")) return value.tuple;
      throw new FenceError("malformed", `Malformed tuple for ${JSON.stringify(key)} (line ${line + 1}).`, line);
    }
    if (Array.isArray(value)) {
      if (value.every((v) => typeof v === "number")) return value;
      if (value.every((v) => typeof v === "string")) return value;
      throw new FenceError("malformed", `Mixed list for ${JSON.stringify(key)} (line ${line + 1}).`, line);
    }
    return value;
  }
  function parseBlock(source) {
    const range = findFence(source);
    if (!range) return null;
    const lines = splitLines(source);
    const body = lines.slice(range.start + 1, range.end);
    const settings = defaultSettings();
    let sawImport = false;
    let i = 0;
    while (i < body.length) {
      const line = body[i];
      const lineNo = range.start + 1 + i;
      const t = line.trim();
      if (t === "" || t.startsWith("#")) {
        i++;
        continue;
      }
      if (t === "import matplotlib as mpl") {
        sawImport = true;
        i++;
        continue;
      }
      const styleMatch = STYLE_RE.exec(line);
      if (styleMatch) {
        try {
          settings.style = parsePyString(styleMatch[1]);
        } catch (e) {
          throw new FenceError("malformed", `Could not read the style name on line ${lineNo + 1}: ${e.message}`, lineNo);
        }
        i++;
        continue;
      }
      if (t.startsWith("mpl.rcParams.update(")) {
        const rest = body.slice(i).join("\n");
        const open = rest.indexOf("(");
        let parsed;
        try {
          parsed = parsePyDict(rest.slice(open + 1));
        } catch (e) {
          if (e instanceof PyLitError) {
            throw new FenceError("malformed", `Could not read the rcParams block starting on line ${lineNo + 1}: ${e.message}`, lineNo);
          }
          throw e;
        }
        const afterDict = rest.slice(open + 1 + parsed.end);
        const close = /^\s*\)/.exec(afterDict);
        if (!close) throw new FenceError("malformed", `rcParams.update starting on line ${lineNo + 1} is missing its closing ")".`, lineNo);
        const afterCall = afterDict.slice(close[0].length);
        const sameLine = afterCall.split("\n")[0];
        if (sameLine.trim() !== "") {
          throw new FenceError("malformed", `Unexpected text after rcParams.update(...): ${JSON.stringify(sameLine.trim())}`, lineNo);
        }
        for (const [key, value] of Object.entries(parsed.value)) {
          settings.rc[key] = fromPyValue(key, value, lineNo);
        }
        const consumed = rest.length - afterCall.length + sameLine.length;
        i += rest.slice(0, consumed).split("\n").length;
        continue;
      }
      throw new FenceError("malformed", `Line ${lineNo + 1} is not something the style panel writes: ${JSON.stringify(t)}`, lineNo);
    }
    if (!sawImport && (settings.style !== "default" || Object.keys(settings.rc).length)) {
      throw new FenceError("malformed", `The block is missing "import matplotlib as mpl".`, range.start);
    }
    const unknownKeys = Object.keys(settings.rc).filter((k) => !CONTROL_FOR_KEY.has(k));
    return { settings, range, unknownKeys };
  }
  const ENCODING_RE = /^#.*coding[:=]/;
  const FUTURE_RE = /^from\s+__future__\s+import\b/;
  const DOCSTRING_OPEN_RE = /^[rRuUbB]{0,2}("""|''')/;
  function insertionIndex(lines) {
    let i = 0;
    let lastHeader = 0;
    while (i < lines.length) {
      const line = lines[i];
      const t = line.trim();
      if (i === 0 && t.startsWith("#!")) {
        i++;
        lastHeader = i;
        continue;
      }
      if (i < 2 && ENCODING_RE.test(t)) {
        i++;
        lastHeader = i;
        continue;
      }
      if (t === "") {
        i++;
        continue;
      }
      if (t.startsWith("#")) {
        i++;
        lastHeader = i;
        continue;
      }
      const doc = DOCSTRING_OPEN_RE.exec(t);
      if (doc && lastHeaderIsOnlyComments(lines, i)) {
        const quote = doc[1];
        const afterOpen = t.slice(doc[0].length);
        let j = i;
        if (!afterOpen.includes(quote)) {
          j = i + 1;
          while (j < lines.length && !lines[j].includes(quote)) j++;
          if (j >= lines.length) return lastHeader;
        }
        i = j + 1;
        lastHeader = i;
        continue;
      }
      if (FUTURE_RE.test(t)) {
        let j = i;
        if (t.includes("(") && !t.includes(")")) {
          while (j < lines.length && !lines[j].includes(")")) j++;
        } else if (t.endsWith("\\")) {
          while (j < lines.length && lines[j].trimEnd().endsWith("\\")) j++;
        }
        i = j + 1;
        lastHeader = i;
        continue;
      }
      break;
    }
    return Math.min(lastHeader, lines.length);
  }
  function lastHeaderIsOnlyComments(lines, upto) {
    for (let k = 0; k < upto; k++) {
      const t = lines[k].trim();
      if (t !== "" && !t.startsWith("#")) return false;
    }
    return true;
  }
  function upsertBlock(source, settings) {
    const block = generateBlock(settings);
    const range = findFence(source);
    if (block === null) return range ? removeRange(source, range) : source;
    const eol = detectEol(source);
    const lines = splitLines(source);
    const blockLines = block.split("\n");
    if (range) {
      lines.splice(range.start, range.end - range.start + 1, ...blockLines);
      return lines.join(eol);
    }
    const at = insertionIndex(lines);
    const insert = [...blockLines];
    if (at > 0 && lines[at - 1].trim() !== "") insert.unshift("");
    if (at < lines.length && lines[at].trim() !== "") insert.push("");
    lines.splice(at, 0, ...insert);
    return lines.join(eol);
  }
  function removeBlock(source) {
    const range = findFence(source);
    return range ? removeRange(source, range) : source;
  }
  function replaceFence(source, settings) {
    const lines = splitLines(source);
    const eol = detectEol(source);
    const start = lines.findIndex((l) => l.trim() === FENCE_START);
    let end = -1;
    for (let i = lines.length - 1; i >= 0; i--) {
      if (lines[i].trim() === FENCE_END) {
        end = i;
        break;
      }
    }
    if (start === -1 || end === -1 || end < start) {
      const cleaned = lines.filter((l) => l.trim() !== FENCE_START && l.trim() !== FENCE_END).join(eol);
      return upsertBlock(cleaned, settings);
    }
    const block = generateBlock(settings);
    lines.splice(start, end - start + 1, ...block === null ? [] : block.split("\n"));
    return lines.join(eol);
  }
  function removeRange(source, range) {
    const eol = detectEol(source);
    const lines = splitLines(source);
    let count = range.end - range.start + 1;
    if (range.end + 1 < lines.length && lines[range.end + 1].trim() === "") count++;
    lines.splice(range.start, count);
    if (range.start === 0) while (lines.length && lines[0].trim() === "") lines.shift();
    return lines.join(eol);
  }
  const HELPER_SOURCE = '"""plotpolish helper — the matplotlib-facing half of the panel.\n\nDesign constraints (see docs/design.md):\n\n* This file is inlined **verbatim** into the JavaScript bundle and executed in\n  a throwaway namespace by the host, so it must be a single self-contained\n  file: no intra-package imports, no reading of data files, no global state.\n* Pure functions, JSON-friendly values in and out. Imports are limited to\n  ``matplotlib``, ``json`` and the standard library.\n* Nothing here ever touches axis labels, titles, limits, scales or\n  annotations. Those belong to the user\'s code. The one per-series exception\n  is ``axes.prop_cycle``: its color/linewidth/linestyle are applied to each\n  line through the property cycle (see ``_apply_prop_cycle``), but a line\n  whose own value was set explicitly and no longer matches the old cycle is\n  left alone under ``only_defaults``.\n"""\n\nimport json\nimport math\nimport traceback\n\nimport matplotlib as mpl\nfrom matplotlib import pyplot as plt\nfrom matplotlib import ticker as _ticker\nfrom matplotlib.colors import to_rgba as _to_rgba\nfrom matplotlib.font_manager import font_scalings as _FONT_SCALINGS\n\n__version__ = "0.2.0"\n\nTOOL_NAME = "plotpolish"\n\n# rc keys the panel knows about. Kept in sync with src/schema/controls.json by\n# python/tests/test_schema_contract.py.\nCURATED_KEYS = (\n    "figure.autolayout",\n    "savefig.dpi",\n    "savefig.transparent",\n    "savefig.bbox",\n    "font.size",\n    "font.family",\n    "axes.titlesize",\n    "axes.labelsize",\n    "xtick.labelsize",\n    "ytick.labelsize",\n    "legend.fontsize",\n    "lines.linewidth",\n    "lines.linestyle",\n    "lines.marker",\n    "lines.markersize",\n    "axes.prop_cycle",\n    "axes.grid",\n    "axes.grid.which",\n    "grid.alpha",\n    "grid.linestyle",\n    "axes.spines.top",\n    "axes.spines.right",\n    "axes.linewidth",\n    "xtick.direction",\n    "ytick.direction",\n    "xtick.minor.visible",\n    "ytick.minor.visible",\n    "legend.frameon",\n    "legend.loc",\n    "legend.framealpha",\n)\n\n# Keys that savefig reads at save time. They have no artist-level equivalent,\n# but setting the rcParam is an honest "live" apply for them.\nSAVE_KEYS = ("savefig.dpi", "savefig.transparent", "savefig.bbox")\n\n# Keys that only take effect when artists are created: need a host re-run. (None today; the style sheet is handled by set_style.)\nRERUN_KEYS = ()\n\n_REL_TOL = 1e-6\n\n\n# --------------------------------------------------------------------------\n# Value conversion between rcParams and JSON\n# --------------------------------------------------------------------------\n\ndef _plain(value):\n    """Turn numpy scalars / tuples into plain JSON-serializable Python."""\n    if hasattr(value, "item") and not isinstance(value, (str, bytes)):\n        try:\n            return value.item()\n        except (TypeError, ValueError):\n            pass\n    if isinstance(value, (list, tuple)):\n        return [_plain(v) for v in value]\n    return value\n\n\ndef rc_to_json(key, value):\n    """Serialize one rcParam value the way the panel expects it.\n\n    ``axes.prop_cycle`` is either a plain list of color strings (legacy,\n    colors only — the cycler\'s only key is ``"color"``) or, once per-line\n    width/style are involved, a dict ``{"color": [...], "linewidth": [...],\n    "linestyle": [...]}`` with ``linewidth``/``linestyle`` present only when\n    the cycler actually carries them.\n    """\n    if key == "axes.prop_cycle":\n        items = list(value)\n        keys = set(value.keys)\n        if keys == {"color"}:\n            return [c["color"] for c in items]\n        result = {"color": [c["color"] for c in items]} if "color" in keys else {"color": []}\n        if "linewidth" in keys:\n            result["linewidth"] = [float(c["linewidth"]) for c in items]\n        if "linestyle" in keys:\n            result["linestyle"] = [str(c["linestyle"]) for c in items]\n        return result\n    if key == "font.family":\n        if isinstance(value, (list, tuple)):\n            return str(value[0]) if value else "sans-serif"\n        return str(value)\n    if key == "savefig.bbox":\n        return "standard" if value is None else str(value)\n    if key == "savefig.dpi":\n        return "figure" if value == "figure" else float(value)\n    if key == "legend.loc":\n        if isinstance(value, (list, tuple)):\n            return [float(value[0]), float(value[1])]\n        return value\n    value = _plain(value)\n    if isinstance(value, float) and value.is_integer():\n        return value  # keep floats as floats; JSON will render 1.0 as 1.0\n    return value\n\n\ndef json_to_rc(key, value):\n    """Inverse of :func:`rc_to_json`: a value that ``mpl.rcParams[key] = …`` accepts."""\n    if key == "axes.prop_cycle":\n        if isinstance(value, dict):\n            kw = {\n                k: ([float(x) for x in v] if k == "linewidth" else list(v))\n                for k, v in value.items()\n                if k in ("color", "linewidth", "linestyle") and v\n            }\n            lengths = {k: len(v) for k, v in kw.items()}\n            if len(set(lengths.values())) > 1:\n                raise ValueError(\n                    "axes.prop_cycle: color/linewidth/linestyle must all be the same "\n                    "length, got %s" % lengths\n                )\n            return mpl.cycler(**kw)\n        return mpl.cycler(color=list(value))\n    if key == "legend.loc" and isinstance(value, (list, tuple)):\n        return tuple(float(v) for v in value)\n    return value\n\n\ndef resolve_size(value, base=None):\n    """Font size in points: numbers pass through, relative names scale ``base``."""\n    if base is None:\n        base = float(mpl.rcParams["font.size"])\n    if isinstance(value, str):\n        if value in _FONT_SCALINGS:\n            return float(base) * float(_FONT_SCALINGS[value])\n        return float(value)  # "12" style strings\n    return float(value)\n\n\ndef _close(a, b):\n    try:\n        return math.isclose(float(a), float(b), rel_tol=_REL_TOL, abs_tol=1e-9)\n    except (TypeError, ValueError):\n        return a == b\n\n\n# --------------------------------------------------------------------------\n# list_styles\n# --------------------------------------------------------------------------\n\ndef list_styles():\n    """Names accepted by ``mpl.style.use``, ``"default"`` first, internals hidden."""\n    names = sorted(\n        (s for s in plt.style.available if not s.startswith("_")),\n        key=str.lower,\n    )\n    return ["default"] + names\n\n\ndef set_style(name, keep=None):\n    """Reset the session\'s rcParams to library defaults, then apply style ``name``.\n\n    The fenced block deliberately does **not** call ``mpl.style.use("default")``:\n    in a long-lived interpreter that would also wipe rc values the host sets\n    before each run (Trinket sets ``figure.autolayout`` and a pane-fitting\n    ``figure.figsize``). Instead the panel calls this *between* runs when the\n    user changes the style dropdown, so leftovers from a previously applied\n    style do not survive into the next run. ``keep`` lists rc keys to\n    preserve across the reset, for hosts that set them once at startup\n    rather than per run.\n\n    Returns the effective values of the curated keys after the change.\n    """\n    keep = list(keep or [])\n    saved = {k: mpl.rcParams[k] for k in keep if k in mpl.rcParams}\n    mpl.style.use("default")\n    if name and name != "default":\n        mpl.style.use(name)\n    mpl.rcParams.update(saved)\n    return {k: rc_to_json(k, mpl.rcParams[k]) for k in CURATED_KEYS}\n\n\n# --------------------------------------------------------------------------\n# Figure access and description\n# --------------------------------------------------------------------------\n\ndef current_figure():\n    """The most recently created open figure, or ``None`` — never creates one."""\n    nums = plt.get_fignums()\n    if not nums:\n        return None\n    return plt.figure(nums[-1])\n\n\ndef _grid_on(axis):\n    try:\n        params = axis.get_tick_params(which="major")\n        if "gridOn" in params:\n            return bool(params["gridOn"])\n    except Exception:  # pragma: no cover - very old/odd backends\n        pass\n    lines = axis.get_gridlines()\n    return bool(lines and lines[0].get_visible())\n\n\ndef _gridline(axis):\n    lines = axis.get_gridlines()\n    return lines[0] if lines else None\n\n\ndef _tick_direction(axis):\n    ticks = axis.get_major_ticks(numticks=1)\n    if ticks and hasattr(ticks[0], "_tickdir"):\n        return ticks[0]._tickdir\n    return None\n\n\ndef _minor_visible(axis):\n    return not isinstance(axis.get_minor_locator(), _ticker.NullLocator)\n\n\ndef _tick_label_size(axis):\n    labels = axis.get_ticklabels()\n    if labels:\n        return float(labels[0].get_fontsize())\n    return None\n\n\ndef _legend_loc_name(legend):\n    loc = getattr(legend, "_loc", None)\n    if isinstance(loc, int):\n        for name, code in legend.codes.items():\n            if code == loc:\n                return name\n    if isinstance(loc, (list, tuple)):\n        return [float(loc[0]), float(loc[1])]\n    return loc if isinstance(loc, str) else None\n\n\ndef _legend_xy(fig, ax, leg):\n    """Lower-left corner of the drawn legend box, in axes fractions, or ``None``."""\n    try:\n        if hasattr(fig.canvas, "get_renderer"):\n            renderer = fig.canvas.get_renderer()\n        else:\n            fig.canvas.draw()\n            renderer = None\n        if renderer is None:\n            return None\n        bb = leg.get_window_extent(renderer)\n        x, y = ax.transAxes.inverted().transform((bb.x0, bb.y0))\n        return [round(float(x), 3), round(float(y), 3)]\n    except Exception:\n        return None\n\n\ndef _describe_axes(ax, fig):\n    leg = ax.get_legend()\n    gx = _gridline(ax.xaxis)\n    return {\n        "grid": _grid_on(ax.xaxis) or _grid_on(ax.yaxis),\n        "grid_alpha": None if gx is None else _plain(gx.get_alpha()),\n        "grid_linestyle": None if gx is None else gx.get_linestyle(),\n        "spines": {name: bool(sp.get_visible()) for name, sp in ax.spines.items()},\n        "axes_linewidth": _plain(next(iter(ax.spines.values())).get_linewidth()) if ax.spines else None,\n        "tick_direction": {"x": _tick_direction(ax.xaxis), "y": _tick_direction(ax.yaxis)},\n        "minor_ticks": {"x": _minor_visible(ax.xaxis), "y": _minor_visible(ax.yaxis)},\n        "title_size": float(ax.title.get_fontsize()),\n        "label_size": {"x": float(ax.xaxis.label.get_fontsize()), "y": float(ax.yaxis.label.get_fontsize())},\n        "tick_label_size": {"x": _tick_label_size(ax.xaxis), "y": _tick_label_size(ax.yaxis)},\n        "n_lines": len(ax.lines),\n        "legend": None if leg is None else {\n            "frameon": bool(leg.get_frame_on()),\n            "framealpha": _plain(leg.get_frame().get_alpha()),\n            "loc": _legend_loc_name(leg),\n            "fontsize": float(leg.get_texts()[0].get_fontsize()) if leg.get_texts() else None,\n            "xy": _legend_xy(fig, ax, leg),\n        },\n    }\n\n\ndef _describe_figure(fig):\n    w, h = fig.get_size_inches()\n    return {\n        "figsize": [float(w), float(h)],\n        "dpi": float(fig.dpi),\n        "autolayout": _layout_is_tight(fig),\n        "axes": [_describe_axes(ax, fig) for ax in fig.axes],\n    }\n\n\n# --------------------------------------------------------------------------\n# Override detection: which rc keys does the figure disagree with?\n# --------------------------------------------------------------------------\n\ndef _find_overrides(fig, rc):\n    """rc keys whose effective value differs from what the live figure shows.\n\n    Heuristic: if the figure\'s artists do not sit at the current rcParam\n    value, the user\'s code (or a style applied after creation) set them.\n    A user who explicitly passes the default value is indistinguishable\n    from one who did not; that is documented behavior.\n    """\n    over = set()\n    base = float(mpl.rcParams["font.size"])\n\n    def differs(key, actual, expected=None):\n        if key == "axes.prop_cycle":\n            return  # per-line values vs. the cycle is apply_live\'s job, not a badge\n        if key not in rc or actual is None:\n            return\n        exp = rc[key] if expected is None else expected\n        if isinstance(exp, bool) or isinstance(actual, bool):\n            if bool(actual) != bool(exp):\n                over.add(key)\n        elif isinstance(exp, (list, tuple)) or isinstance(actual, (list, tuple)):\n            if not _close_or_equal(actual, exp):\n                over.add(key)\n        elif isinstance(exp, (int, float)) and not isinstance(exp, bool):\n            if not _close(actual, exp):\n                over.add(key)\n        elif actual != exp:\n            over.add(key)\n\n    def size_differs(key, actual):\n        if key in rc and actual is not None:\n            differs(key, actual, resolve_size(rc[key], base))\n\n    differs("figure.autolayout", _layout_is_tight(fig))\n\n    for ax in fig.axes:\n        differs("axes.grid", _grid_on(ax.xaxis) or _grid_on(ax.yaxis))\n        gx = _gridline(ax.xaxis)\n        if gx is not None:\n            differs("grid.alpha", gx.get_alpha() if gx.get_alpha() is not None else 1.0)\n            differs("grid.linestyle", gx.get_linestyle())\n        for name in ("top", "right"):\n            if name in ax.spines:\n                differs("axes.spines." + name, ax.spines[name].get_visible())\n        for sp in ax.spines.values():\n            differs("axes.linewidth", sp.get_linewidth())\n        differs("xtick.direction", _tick_direction(ax.xaxis))\n        differs("ytick.direction", _tick_direction(ax.yaxis))\n        differs("xtick.minor.visible", _minor_visible(ax.xaxis))\n        differs("ytick.minor.visible", _minor_visible(ax.yaxis))\n        size_differs("axes.titlesize", ax.title.get_fontsize())\n        if ax.title.get_text():\n            differs("font.family", _family_name(ax.title.get_fontfamily()))\n        size_differs("axes.labelsize", ax.xaxis.label.get_fontsize())\n        size_differs("axes.labelsize", ax.yaxis.label.get_fontsize())\n        size_differs("xtick.labelsize", _tick_label_size(ax.xaxis))\n        size_differs("ytick.labelsize", _tick_label_size(ax.yaxis))\n        for line in ax.lines:\n            differs("lines.linewidth", line.get_linewidth())\n            differs("lines.linestyle", line.get_linestyle())\n            differs("lines.marker", str(line.get_marker()))\n            differs("lines.markersize", line.get_markersize())\n        leg = ax.get_legend()\n        if leg is not None:\n            differs("legend.frameon", leg.get_frame_on())\n            alpha = leg.get_frame().get_alpha()\n            if alpha is not None:\n                differs("legend.framealpha", alpha)\n            loc = _legend_loc_name(leg)\n            if loc is not None:\n                differs("legend.loc", loc)\n            if leg.get_texts():\n                size_differs("legend.fontsize", leg.get_texts()[0].get_fontsize())\n    return sorted(over)\n\n\ndef introspect_figure(keys=None):\n    """Effective rc values, defaults, and what the live figure actually shows.\n\n    Returns a JSON-friendly dict::\n\n        {"matplotlib": "3.8.4",\n         "rc": {key: value},          # mpl.rcParams, for the requested keys\n         "defaults": {key: value},    # mpl.rcParamsDefault, same keys\n         "figure": {...} | None,      # artist-level description, if a figure exists\n         "overridden": [key, ...]}    # keys the figure disagrees with\n\n    ``rc`` includes whatever style sheet the user\'s code applied, but nothing\n    records *which* one: that is why the fenced block, not introspection, is\n    the panel\'s source of truth for the style name.\n    """\n    keys = list(keys) if keys else list(CURATED_KEYS)\n    rc = {k: rc_to_json(k, mpl.rcParams[k]) for k in keys if k in mpl.rcParams}\n    defaults = {k: rc_to_json(k, mpl.rcParamsDefault[k]) for k in keys if k in mpl.rcParamsDefault}\n    result = {\n        "matplotlib": mpl.__version__,\n        "rc": rc,\n        "defaults": defaults,\n        "figure": None,\n        "overridden": [],\n    }\n    fig = current_figure()\n    if fig is not None:\n        result["figure"] = _describe_figure(fig)\n        result["overridden"] = _find_overrides(fig, rc)\n    return result\n\n\n# --------------------------------------------------------------------------\n# apply_live: artist-level equivalents for live preview\n# --------------------------------------------------------------------------\n\ndef _set_if_default(getter, setter, old, new, only_defaults):\n    """Apply ``setter(new)`` unless the artist was deliberately moved off ``old``."""\n    if only_defaults:\n        current = getter()\n        if current is not None and not _close_or_equal(current, old):\n            return False\n    setter(new)\n    return True\n\n\ndef _close_or_equal(a, b):\n    if isinstance(a, (list, tuple)) and isinstance(b, (list, tuple)):\n        return len(a) == len(b) and all(_close_or_equal(x, y) for x, y in zip(a, b))\n    if isinstance(a, (int, float)) and isinstance(b, (int, float)) and not isinstance(a, bool):\n        return _close(a, b)\n    return a == b\n\n\ndef _layout_state(fig):\n    """\'tight\', \'none\' (no engine or the placeholder), or \'other\' (constrained, compressed, custom)."""\n    engine = fig.get_layout_engine()\n    if engine is None:\n        return "none"\n    name = type(engine).__name__\n    if name == "TightLayoutEngine":\n        return "tight"\n    if name == "PlaceHolderLayoutEngine":\n        return "none"\n    return "other"\n\n\ndef _layout_is_tight(fig):\n    return _layout_state(fig) == "tight"\n\n\ndef _apply_autolayout(fig, new, old, only):\n    """figure.autolayout: tight_layout on every draw, via the figure\'s layout engine.\n\n    A figure the user gave another engine (constrained, compressed) is never\n    switched under them when only_defaults is set.\n    """\n    state = _layout_state(fig)\n    if only and (state == "other" or (state == "tight") != bool(old)):\n        return\n    fig.set_layout_engine("tight" if new else "none")\n\n\ndef _family_name(value):\n    """First family name of a font.family value (str or list)."""\n    if isinstance(value, (list, tuple)):\n        return str(value[0]) if value else "sans-serif"\n    return str(value)\n\n\ndef _apply_font_family(fig, new, old, only):\n    """font.family: set on every Text artist that still uses the old family, and on future tick labels."""\n    from matplotlib.text import Text\n    old_name = _family_name(old)\n    new_name = _family_name(new)\n    for text in fig.findobj(Text):\n        if only and _family_name(text.get_fontfamily()) != old_name:\n            continue\n        text.set_family(new_name)\n    for ax in fig.axes:\n        try:\n            ax.tick_params(axis="both", which="both", labelfontfamily=new_name)\n        except (AttributeError, TypeError, ValueError):  # pragma: no cover - matplotlib < 3.7\n            pass\n\n\ndef _apply_grid(fig, new, old, only):\n    for ax in fig.axes:\n        current = _grid_on(ax.xaxis) or _grid_on(ax.yaxis)\n        if only and current != bool(old):\n            continue\n        ax.grid(bool(new))\n\n\ndef _apply_grid_which(fig, new, old, only):\n    """Draw grid lines at the minor ticks as well as the major ones.\n\n    matplotlib only draws a minor grid line where a minor tick exists, so this\n    does nothing visible unless xtick/ytick.minor.visible are on -- the panel\n    says so in the control\'s help rather than silently turning them on, which\n    would be a second change the user did not ask for.\n    """\n    which = str(new)\n    minor_on = which in ("minor", "both")\n    for ax in fig.axes:\n        for axis in (ax.xaxis, ax.yaxis):\n            if only:\n                current = _grid_on(axis)\n                if not current:\n                    continue\n            for tick in axis.get_minor_ticks():\n                tick.gridline.set_visible(minor_on)\n        # Keep the major grid as it was; ax.grid(which=...) would toggle it.\n        ax.tick_params(axis="both", which="minor", gridOn=minor_on)\n\n\ndef _apply_grid_kw(kw):\n    def apply(fig, new, old, only):\n        for ax in fig.axes:\n            gx = _gridline(ax.xaxis)\n            if only and gx is not None:\n                current = gx.get_alpha() if kw == "alpha" else gx.get_linestyle()\n                if current is not None and not _close_or_equal(current, old):\n                    continue\n            # tick_params(grid_*) changes gridline properties without turning\n            # the grid on, unlike ax.grid(**kwargs).\n            ax.tick_params(axis="both", which="both", **{"grid_" + kw: new})\n    return apply\n\n\ndef _apply_spine_visible(name):\n    def apply(fig, new, old, only):\n        for ax in fig.axes:\n            sp = ax.spines.get(name)\n            if sp is None:\n                continue\n            if only and sp.get_visible() != bool(old):\n                continue\n            sp.set_visible(bool(new))\n    return apply\n\n\ndef _apply_axes_linewidth(fig, new, old, only):\n    for ax in fig.axes:\n        for sp in ax.spines.values():\n            if only and not _close(sp.get_linewidth(), old):\n                continue\n            sp.set_linewidth(float(new))\n\n\ndef _apply_tick_direction(axis_name):\n    def apply(fig, new, old, only):\n        for ax in fig.axes:\n            axis = getattr(ax, axis_name + "axis")\n            current = _tick_direction(axis)\n            if only and current is not None and current != old:\n                continue\n            ax.tick_params(axis=axis_name, which="both", direction=str(new))\n    return apply\n\n\ndef _apply_minor_visible(axis_name):\n    def apply(fig, new, old, only):\n        for ax in fig.axes:\n            axis = getattr(ax, axis_name + "axis")\n            if only and _minor_visible(axis) != bool(old):\n                continue\n            if bool(new):\n                if axis.get_scale() != "linear":\n                    continue  # leave log/symlog minor ticks to matplotlib\n                axis.set_minor_locator(_ticker.AutoMinorLocator())\n            else:\n                axis.set_minor_locator(_ticker.NullLocator())\n    return apply\n\n\ndef _apply_text_size(texts_of):\n    """Handler factory for fontsize keys. ``texts_of(ax)`` yields Text artists."""\n    def apply(fig, new, old, only, base_old=None, base_new=None):\n        old_pts = resolve_size(old, base_old)\n        new_pts = resolve_size(new, base_new)\n        for ax in fig.axes:\n            for text in texts_of(ax):\n                if only and not _close(text.get_fontsize(), old_pts):\n                    continue\n                text.set_fontsize(new_pts)\n    return apply\n\n\ndef _apply_tick_label_size(axis_name):\n    def apply(fig, new, old, only, base_old=None, base_new=None):\n        old_pts = resolve_size(old, base_old)\n        new_pts = resolve_size(new, base_new)\n        for ax in fig.axes:\n            axis = getattr(ax, axis_name + "axis")\n            current = _tick_label_size(axis)\n            if only and current is not None and not _close(current, old_pts):\n                continue\n            ax.tick_params(axis=axis_name, which="both", labelsize=new_pts)\n    return apply\n\n\ndef _legend_texts(ax):\n    leg = ax.get_legend()\n    return leg.get_texts() if leg is not None else []\n\n\ndef _colors_equal(a, b):\n    """Color equality that treats equivalent spellings (e.g. case) as equal."""\n    try:\n        return _to_rgba(a) == _to_rgba(b)\n    except (ValueError, TypeError):\n        return a == b\n\n\n# Scalar rc key -> the axes.prop_cycle property that overrides it on a draw.\n# When the cycle carries that property, the scalar must not touch the artists.\n_CYCLE_MASTERS = {"lines.linewidth": "linewidth", "lines.linestyle": "linestyle"}\n\n# axes.prop_cycle property name -> (getter, setter, caster) on a Line2D.\n_PROP_CYCLE_ATTRS = {\n    "color": ("get_color", "set_color", str),\n    "linewidth": ("get_linewidth", "set_linewidth", float),\n    "linestyle": ("get_linestyle", "set_linestyle", str),\n}\n\n\ndef _cycle_props(value):\n    """Normalize an ``axes.prop_cycle`` JSON value to ``{prop: [values, ...]}``.\n\n    ``value`` may be a plain list of colors (legacy), a dict with "color"\n    and optionally "linewidth"/"linestyle", or ``None``/empty. Only keys\n    that are actually present with a non-empty list survive.\n    """\n    if not value:\n        return {}\n    if isinstance(value, (list, tuple)):\n        return {"color": list(value)}\n    return {k: list(v) for k, v in value.items() if k in _PROP_CYCLE_ATTRS and v}\n\n\ndef _apply_prop_cycle(fig, new, old, only):\n    """Apply per-line color/linewidth/linestyle through the property cycle.\n\n    For each axes and each line at index ``i``, the new value for a\n    property comes from ``new``\'s list at ``i % len(list)``; the old value\n    (what the line would already show if it followed the previous cycle)\n    comes the same way from ``old``. When ``old`` carries no entry for a\n    property — no old cycle at all, or an old cycle that never mentioned\n    linewidth/linestyle — the fallback is ``mpl.rcParams``\'s current value\n    for linewidth/linestyle, and the line\'s own current color for color\n    (which trivially "matches", so a first per-line color change is never\n    blocked by ``only_defaults``).\n    """\n    new_props = _cycle_props(new)\n    if not new_props:\n        return\n    old_props = _cycle_props(old)\n\n    for ax in fig.axes:\n        for i, line in enumerate(ax.lines):\n            for prop, new_list in new_props.items():\n                getter_name, setter_name, caster = _PROP_CYCLE_ATTRS[prop]\n                new_val = new_list[i % len(new_list)]\n                old_list = old_props.get(prop)\n                if old_list:\n                    old_val = old_list[i % len(old_list)]\n                elif prop == "color":\n                    old_val = line.get_color()\n                else:\n                    old_val = mpl.rcParams["lines." + prop]\n                if only:\n                    current = getattr(line, getter_name)()\n                    matches = (\n                        _colors_equal(current, old_val) if prop == "color"\n                        else _close_or_equal(current, old_val)\n                    )\n                    if not matches:\n                        continue\n                getattr(line, setter_name)(caster(new_val))\n\n\ndef _apply_line_prop(getter_name, setter_name, caster):\n    def apply(fig, new, old, only):\n        for ax in fig.axes:\n            for line in ax.lines:\n                current = getattr(line, getter_name)()\n                if only and not _close_or_equal(current, old):\n                    continue\n                getattr(line, setter_name)(caster(new))\n    return apply\n\n\ndef _apply_legend_frameon(fig, new, old, only):\n    for ax in fig.axes:\n        leg = ax.get_legend()\n        if leg is None or (only and leg.get_frame_on() != bool(old)):\n            continue\n        leg.set_frame_on(bool(new))\n\n\ndef _apply_legend_framealpha(fig, new, old, only):\n    for ax in fig.axes:\n        leg = ax.get_legend()\n        if leg is None:\n            continue\n        current = leg.get_frame().get_alpha()\n        if only and current is not None and not _close(current, old):\n            continue\n        leg.get_frame().set_alpha(float(new))\n\n\ndef _apply_legend_loc(fig, new, old, only):\n    for ax in fig.axes:\n        leg = ax.get_legend()\n        if leg is None:\n            continue\n        current = _legend_loc_name(leg)\n        if only and current is not None and not _close_or_equal(current, old):\n            continue\n        if hasattr(leg, "set_loc"):\n            if isinstance(new, (list, tuple)):\n                leg.set_loc(tuple(float(v) for v in new))\n            else:\n                leg.set_loc(str(new))\n        else:  # pragma: no cover - matplotlib < 3.8\n            leg._set_loc(leg.codes[str(new)])\n\n\n_TEXT_SIZE_HANDLERS = {\n    "axes.titlesize": _apply_text_size(lambda ax: [ax.title]),\n    "axes.labelsize": _apply_text_size(lambda ax: [ax.xaxis.label, ax.yaxis.label]),\n    "xtick.labelsize": _apply_tick_label_size("x"),\n    "ytick.labelsize": _apply_tick_label_size("y"),\n    "legend.fontsize": _apply_text_size(_legend_texts),\n}\n\n_LIVE_HANDLERS = {\n    "font.family": _apply_font_family,\n    "figure.autolayout": _apply_autolayout,\n    "axes.grid": _apply_grid,\n    "axes.grid.which": _apply_grid_which,\n    "grid.alpha": _apply_grid_kw("alpha"),\n    "grid.linestyle": _apply_grid_kw("linestyle"),\n    "axes.spines.top": _apply_spine_visible("top"),\n    "axes.spines.right": _apply_spine_visible("right"),\n    "axes.linewidth": _apply_axes_linewidth,\n    "xtick.direction": _apply_tick_direction("x"),\n    "ytick.direction": _apply_tick_direction("y"),\n    "xtick.minor.visible": _apply_minor_visible("x"),\n    "ytick.minor.visible": _apply_minor_visible("y"),\n    "lines.linewidth": _apply_line_prop("get_linewidth", "set_linewidth", float),\n    "lines.linestyle": _apply_line_prop("get_linestyle", "set_linestyle", str),\n    "lines.marker": _apply_line_prop("get_marker", "set_marker", str),\n    "lines.markersize": _apply_line_prop("get_markersize", "set_markersize", float),\n    "axes.prop_cycle": _apply_prop_cycle,\n    "legend.frameon": _apply_legend_frameon,\n    "legend.framealpha": _apply_legend_framealpha,\n    "legend.loc": _apply_legend_loc,\n}\n\nLIVE_KEYS = ("font.size",) + tuple(_TEXT_SIZE_HANDLERS) + tuple(_LIVE_HANDLERS)\n\n\ndef apply_live(rc, only_defaults=True, previous=None):\n    """Apply artist-level equivalents of ``rc`` to the live figure.\n\n    ``rc`` maps rc keys to JSON values (as produced by the panel). For every\n    key with an artist-level equivalent the retained figure is updated; for\n    save-time keys the rcParam is set; re-run-only keys are reported back as\n    deferred. ``mpl.rcParams`` is updated for every applied key so that a\n    subsequent call compares against the new baseline. ``legend.loc`` is one\n    such key whose JSON value may be either a named location string or an\n    ``[x, y]`` axes-fraction pair for a custom position.\n\n    With ``only_defaults=True`` (the default) an artist is only changed if it\n    currently sits at the old rc value — a line the user drew with ``lw=3``\n    is left alone, which mirrors what a re-run of their code would do.\n\n    ``previous``, if given, is a dict mapping rc keys to the JSON values the\n    caller believes the retained figure currently sits at (the panel records\n    these from the last ``introspect_figure()[\'rc\']`` plus its own successful\n    applies). It exists because ``set_style()`` resets/moves ``mpl.rcParams``\n    without touching the retained figure: after a style change,\n    ``mpl.rcParams[key]`` no longer describes what the figure\'s artists were\n    drawn against, so comparing against it (the ``only_defaults`` default)\n    makes every artist look user-set and live preview silently stops\n    applying. Passing ``previous`` lets the caller supply the true "old"\n    baseline instead. Keys missing from ``previous`` (or omitted entirely)\n    fall back to today\'s behavior of reading ``mpl.rcParams``.\n    """\n    fig = current_figure()\n    result = {"applied": [], "deferred": [], "unknown": [], "has_figure": fig is not None}\n    rc = dict(rc or {})\n\n    # ``axes.prop_cycle`` supplies per-line linewidth/linestyle, and on a real\n    # draw the cycler wins over the scalar rcParam: matplotlib gives [8, 8] for\n    # a cycler of linewidth=[8, 8] even with lines.linewidth=2. Mirror that.\n    # The scalar is still written to mpl.rcParams, so a re-run and savefig\n    # agree, but it must not walk the artists -- otherwise the "(all)" master\n    # marches over every line and undoes the per-line values the cycler just\n    # applied, and because each applier carries its own only_defaults guard it\n    # undoes them for some lines and not others.\n    cycled = set(_cycle_props(rc.get("axes.prop_cycle"))) if "axes.prop_cycle" in rc else set()\n\n    def old_value(key):\n        if isinstance(previous, dict) and key in previous:\n            return previous[key]\n        return rc_to_json(key, mpl.rcParams[key])\n\n    # font.size first: relative sizes ("large") resolve against it.\n    base_old = float(old_value("font.size"))\n    base_new = float(rc["font.size"]) if "font.size" in rc else base_old\n    if "font.size" in rc:\n        if fig is not None and not _close(base_old, base_new):\n            for key, handler in _TEXT_SIZE_HANDLERS.items():\n                if key in rc:\n                    continue  # explicitly set below\n                current = old_value(key)\n                if isinstance(current, str):  # relative: follows font.size\n                    handler(fig, current, current, only_defaults, base_old=base_old, base_new=base_new)\n        mpl.rcParams["font.size"] = base_new\n        result["applied"].append("font.size")\n\n    for key, value in rc.items():\n        if key == "font.size":\n            continue\n        if key in _TEXT_SIZE_HANDLERS:\n            if fig is not None:\n                _TEXT_SIZE_HANDLERS[key](fig, value, old_value(key), only_defaults,\n                                         base_old=base_old, base_new=base_new)\n            mpl.rcParams[key] = value\n            result["applied"].append(key)\n        elif key in _LIVE_HANDLERS:\n            if fig is not None and _CYCLE_MASTERS.get(key) not in cycled:\n                _LIVE_HANDLERS[key](fig, value, old_value(key), only_defaults)\n            mpl.rcParams[key] = json_to_rc(key, value)\n            result["applied"].append(key)\n        elif key in SAVE_KEYS:\n            mpl.rcParams[key] = json_to_rc(key, value)\n            result["applied"].append(key)\n        elif key in RERUN_KEYS or key not in mpl.rcParams:\n            (result["deferred"] if key in RERUN_KEYS else result["unknown"]).append(key)\n        else:\n            result["unknown"].append(key)\n\n    if fig is not None and result["applied"]:\n        try:\n            fig.canvas.draw_idle()\n        except Exception:  # pragma: no cover - headless canvases without draw_idle\n            pass\n    return result\n\n\n# --------------------------------------------------------------------------\n# JSON entry point used by the JS side\n# --------------------------------------------------------------------------\n\n_DISPATCH = {\n    "list_styles": lambda args: list_styles(),\n    "set_style": lambda args: set_style(**args),\n    "introspect_figure": lambda args: introspect_figure(**args),\n    "apply_live": lambda args: apply_live(**args),\n}\n\n\ndef dispatch(request_json):\n    """``{"fn": name, "args": {...}}`` in, ``{"ok": true, "result": …}`` out (as JSON)."""\n    try:\n        request = json.loads(request_json)\n        fn = _DISPATCH[request["fn"]]\n        result = fn(request.get("args") or {})\n        return json.dumps({"ok": True, "result": result, "version": __version__})\n    except Exception as exc:  # noqa: BLE001 - surfaced to the panel as text\n        return json.dumps({\n            "ok": False,\n            "error": "%s: %s" % (type(exc).__name__, exc),\n            "traceback": traceback.format_exc(),\n            "version": __version__,\n        })\n';
  class BackendError extends Error {
    constructor(fn, message, traceback) {
      super(`${fn}: ${message}`);
      this.name = "BackendError";
      this.fn = fn;
      this.traceback = traceback;
    }
  }
  function buildSnippet(fn, args = {}) {
    const payload = JSON.stringify(JSON.stringify({ fn, args }));
    return `${HELPER_SOURCE}
${RESULT_VARIABLE} = dispatch(${payload})
${RESULT_VARIABLE}
`;
  }
  class HelperClient {
    constructor(backend) {
      this.backend = backend;
    }
    async call(fn, args = {}) {
      const raw2 = await this.backend.runPython(buildSnippet(fn, args));
      let parsed;
      try {
        parsed = JSON.parse(raw2);
      } catch {
        throw new BackendError(fn, `backend did not return JSON: ${String(raw2).slice(0, 200)}`);
      }
      if (!parsed || typeof parsed !== "object" || !("ok" in parsed)) {
        throw new BackendError(fn, `unexpected response shape: ${String(raw2).slice(0, 200)}`);
      }
      if (!parsed.ok) throw new BackendError(fn, parsed.error, parsed.traceback);
      return parsed.result;
    }
    listStyles() {
      return this.call("list_styles");
    }
    /** Reset the session's rcParams and apply `name`; returns the new effective curated values. */
    setStyle(name, keep = []) {
      return this.call("set_style", { name, keep });
    }
    introspect(keys) {
      return this.call("introspect_figure", keys ? { keys } : {});
    }
    /**
     * Apply artist-level equivalents. `previous` tells the helper what the
     * retained figure currently sits at for each key, so the "is this artist
     * still at the default?" test does not depend on `mpl.rcParams`, which
     * `set_style` moves without touching the figure.
     */
    applyLive(rc, onlyDefaults = true, previous) {
      const args = { rc, only_defaults: onlyDefaults };
      if (previous) args.previous = previous;
      return this.call("apply_live", args);
    }
  }
  class MemorySink {
    constructor(initial = "", onChange) {
      this.onChange = onChange;
      this.listeners = /* @__PURE__ */ new Set();
      this.writes = [];
      this.source = initial;
    }
    getSource() {
      return this.source;
    }
    setSource(source) {
      this.source = source;
      this.writes.push(source);
      this.onChange?.(source);
    }
    /** Simulate an external edit (the user typing): updates and notifies subscribers. */
    externalEdit(source) {
      this.source = source;
      for (const l of this.listeners) l();
    }
    subscribe(listener) {
      this.listeners.add(listener);
      return () => this.listeners.delete(listener);
    }
  }
  class ClipboardSink {
    constructor(options = {}) {
      this.options = options;
      this.lastBlock = "";
      this.lastCopied = null;
      this.textarea = null;
    }
    getSource() {
      return null;
    }
    setSource(block) {
      this.lastBlock = block;
      this.render(block);
      const clipboard = this.options.clipboard === void 0 ? (typeof navigator !== "undefined" ? navigator.clipboard : void 0) ?? null : this.options.clipboard;
      if (!clipboard || !block) {
        this.lastCopied = false;
        this.options.onCopied?.(false, block);
        return;
      }
      let attempt;
      try {
        attempt = Promise.resolve(clipboard.writeText(block));
      } catch (e) {
        attempt = Promise.reject(e);
      }
      void attempt.then(() => {
        this.lastCopied = true;
        this.options.onCopied?.(true, block);
      }).catch(() => {
        this.lastCopied = false;
        this.options.onCopied?.(false, block);
      });
    }
    /** The fallback textarea (created on first write) so hosts can place it. */
    get element() {
      return this.textarea;
    }
    render(block) {
      if (typeof document === "undefined") return;
      if (!this.textarea) {
        this.textarea = document.createElement("textarea");
        this.textarea.readOnly = true;
        this.textarea.spellcheck = false;
        this.textarea.setAttribute("aria-label", "Generated plot style block");
        this.textarea.rows = 8;
        this.textarea.addEventListener("focus", () => this.textarea?.select());
        this.options.container?.appendChild(this.textarea);
      }
      this.textarea.value = block;
      this.textarea.placeholder = block ? "" : "No block: every setting is at its default.";
    }
  }
  const css = `/* Scoped styles for <plotpolish-panel>. Hosts theme it through the --sf-* custom
   properties; everything else stays inside the shadow root.

   Shell (v3, see docs/ux-design.md): the host inserts this element inline into
   matplotlib's toolbar row, so :host renders as a small pill that fits a
   ~40px-tall row. Everything else (the popover, the reset menu, the rail
   fallback) is a fixed-position layer that escapes any overflow clipping. */

:host {
  --_fg: var(--sf-fg, #1f2328);
  --_bg: var(--sf-bg, #ffffff);
  --_muted: var(--sf-muted, #59636e);
  --_border: var(--sf-border, #d0d7de);
  --_accent: var(--sf-accent, #0969da);
  --_accent-tint: var(--sf-accent-tint, #ddf0ff);
  --_danger: var(--sf-danger, #cf222e);
  --_field-bg: var(--sf-field-bg, #ffffff);
  --_section-bg: var(--sf-section-bg, #f6f8fa);
  --_error-bg: var(--sf-error-bg, #ffebe9);
  --_font: var(--sf-font, system-ui, -apple-system, "Segoe UI", Roboto, sans-serif);
  --_mono: var(--sf-mono, ui-monospace, SFMono-Regular, Menlo, Consolas, monospace);

  display: inline-flex;
  vertical-align: middle;
  box-sizing: border-box;
  color: var(--_fg);
  font: 12px/1.4 var(--_font);
}

:host([theme="dark"]) {
  --_fg: var(--sf-fg, #e6edf3);
  --_bg: var(--sf-bg, #0d1117);
  --_muted: var(--sf-muted, #9198a1);
  --_border: var(--sf-border, #3d444d);
  --_accent: var(--sf-accent, #4493f8);
  --_accent-tint: var(--sf-accent-tint, #12395c);
  --_danger: var(--sf-danger, #ff7b72);
  --_field-bg: var(--sf-field-bg, #151b23);
  --_section-bg: var(--sf-section-bg, #161b22);
  --_error-bg: var(--sf-error-bg, #4a1c1c);
}

@media (prefers-color-scheme: dark) {
  :host(:not([theme="light"])) {
    --_fg: var(--sf-fg, #e6edf3);
    --_bg: var(--sf-bg, #0d1117);
    --_muted: var(--sf-muted, #9198a1);
    --_border: var(--sf-border, #3d444d);
    --_accent: var(--sf-accent, #4493f8);
    --_accent-tint: var(--sf-accent-tint, #12395c);
    --_danger: var(--sf-danger, #ff7b72);
    --_field-bg: var(--sf-field-bg, #151b23);
    --_section-bg: var(--sf-section-bg, #161b22);
    --_error-bg: var(--sf-error-bg, #4a1c1c);
  }
}

*, *::before, *::after { box-sizing: inherit; }
[hidden] { display: none !important; }

button {
  font: inherit; color: var(--_fg); background: var(--_field-bg);
  border: 1px solid var(--_border); border-radius: 6px; cursor: pointer;
}
button:hover { border-color: var(--_accent); }
button:disabled { opacity: 0.5; cursor: default; }
button.small { padding: 0 6px; font-size: 11px; line-height: 20px; }

/* --- Tab pill --- */
.pill {
  display: inline-flex; align-items: center; gap: 4px; height: 26px;
  padding: 0 4px; border-radius: 999px; background: var(--_accent-tint);
  border: 1px solid var(--_border);
}
.pill.error { border-color: var(--_danger); }
.pill .err {
  color: var(--_danger); font-weight: 700; padding: 0 4px; cursor: default;
  border: 0; background: transparent;
}

/* Floating pill (layout="float"): a fixed layer over the figure's top-right
   corner, given a stronger presence so it reads over a plot. */
.pill.float {
  position: fixed; z-index: 2147483000;
  border: 1.5px solid var(--_accent);
  box-shadow: 0 4px 14px rgba(0, 0, 0, 0.22);
  background: var(--_bg);
  background-clip: padding-box;
}
.pill.float::before {
  content: ""; position: absolute; inset: 0; border-radius: 999px;
  background: var(--_bg); opacity: 0.96; z-index: -1;
}
/* Dragging the pill (from its grip): stronger shadow while it moves, like the popover. */
.pill.dragging { box-shadow: 0 10px 28px rgba(0, 0, 0, 0.28); }

.pill .grip {
  color: var(--_muted); font-size: 11px; line-height: 1; letter-spacing: -1px;
  user-select: none; cursor: grab; padding: 0 2px; touch-action: none;
}
.pill.dragging .grip { cursor: grabbing; }

button.tab {
  position: relative; height: 22px; padding: 0 9px; border: 0; border-radius: 999px;
  background: transparent; color: var(--_fg); font-size: 12px; line-height: 22px;
  display: inline-flex; align-items: center; gap: 4px; white-space: nowrap;
}
button.tab[aria-selected="true"] { background: var(--_accent); color: #fff; }
button.tab .dot {
  width: 5px; height: 5px; border-radius: 50%; background: currentColor; opacity: 0.7;
}
button.tab .rerun { font-size: 10px; line-height: 1; }

button.menu-toggle {
  height: 22px; padding: 0 6px; border: 0; border-radius: 999px; background: transparent;
  font-size: 11px; color: var(--_muted);
}
button.menu-toggle:hover { color: var(--_accent); }

/* --- Rail fallback --- */
.rail {
  position: fixed; z-index: 2147483000; display: flex; flex-direction: column; gap: 2px;
  padding: 4px; border-radius: 8px; background: var(--_accent-tint); border: 1px solid var(--_border);
}
.rail.error { border-color: var(--_danger); }
.rail button.tab { width: 100%; border-radius: 6px; justify-content: flex-start; }

/* --- Popover --- */
.popover {
  position: fixed; z-index: 2147483000; width: max-content; min-width: 260px; max-width: 380px;
  padding: 8px; border-radius: 8px; background: var(--_bg); opacity: 1; background-clip: padding-box;
  border: 1px solid var(--_border); box-shadow: 0 6px 20px rgba(0, 0, 0, 0.18);
  font-size: 12px;
}
.popover::before { content: ""; position: absolute; inset: 0; border-radius: 8px; background: var(--_bg); opacity: 0.96; z-index: -1; }
.popover.dragging { box-shadow: 0 10px 28px rgba(0, 0, 0, 0.28); }

.caret {
  position: absolute; bottom: -6px; width: 12px; height: 6px; overflow: hidden;
}
.caret::after {
  content: ""; position: absolute; top: -6px; left: 0; width: 12px; height: 12px;
  background: var(--_bg); border: 1px solid var(--_border); transform: rotate(45deg);
}
/* Popover opened below its tab: caret on the top edge, pointing up. */
.popover.below .caret { bottom: auto; top: -6px; }
.popover.below .caret::after { top: 0; }

.pop-head { display: flex; align-items: center; gap: 4px; min-height: 22px; cursor: grab; touch-action: none; margin: 0 0 6px; }
.popover.dragging .pop-head { cursor: grabbing; }
.pop-head .grip { color: var(--_muted); font-size: 11px; line-height: 1; letter-spacing: -1px; user-select: none; }
.pop-head .title { flex: 1; font-size: 12px; font-weight: 700; }
.pop-head button { border: 0; background: transparent; color: var(--_muted); font-size: 13px; line-height: 1; padding: 2px 4px; }
.pop-head button:hover { color: var(--_accent); }

.pop-body .banner.error {
  padding: 6px 8px; border-radius: 6px; background: var(--_error-bg); border: 1px solid var(--_danger);
  font-size: 11.5px;
}
.pop-body .banner button { margin-left: 6px; }

.note { margin: 0 0 6px; padding: 4px 8px; border-radius: 6px; background: var(--_section-bg); color: var(--_muted); }
.unknown-note { margin: 6px 0 0; }
.muted { color: var(--_muted); font-size: 11px; }

.rows { border: 1px solid var(--_border); border-radius: 8px; overflow: hidden; }
.rows:empty { display: none; border: 0; }
.rows.more { margin-top: 4px; }

.subhead {
  margin: 0; padding: 3px 8px; font-size: 10px; font-weight: 600; letter-spacing: 0.02em;
  text-transform: uppercase; color: var(--_muted); background: var(--_section-bg);
  border-bottom: 1px solid var(--_border);
}

button.more {
  display: block; width: 100%; text-align: left; margin-top: 4px; border: 0; background: transparent;
  color: var(--_muted); font-size: 11.5px; padding: 2px 2px;
}
button.more:hover { color: var(--_accent); }

.row {
  display: grid; grid-template-columns: minmax(80px, 1fr) auto; align-items: center;
  gap: 2px 4px; padding: 3px 8px; min-height: 26px; border-bottom: 1px solid var(--_border);
}
.row:last-child { border-bottom: 0; }
.row label { display: block; color: var(--_muted); font-size: 12px; }
.row.is-set label::after {
  content: ""; display: inline-block; width: 5px; height: 5px; margin-left: 5px;
  border-radius: 50%; background: var(--_accent); vertical-align: middle;
}
.row .badges { grid-column: 1; display: flex; gap: 3px; }
.row .control { grid-column: 2; grid-row: 1 / span 2; display: flex; align-items: center; gap: 3px; justify-content: flex-end; }

.badge {
  display: inline-flex; align-items: center; justify-content: center; font-size: 10px; line-height: 1;
  color: var(--_muted);
}
.badge.rerun { color: var(--_accent); font-size: 11px; }
.badge.user {
  width: 5px; height: 5px; border-radius: 50%; background: var(--_muted); padding: 0; border: 0;
}

input[type="number"], select {
  font: inherit; color: var(--_fg); background: var(--_field-bg);
  border: 1px solid var(--_border); border-radius: 5px; padding: 1px 4px; min-width: 0;
}
input[type="number"] { width: 52px; }
/* Every remaining number input is a per-line width cell; its spinner buttons
   add nothing at 48px wide, so they are hidden everywhere. */
input[type="number"] { appearance: textfield; -moz-appearance: textfield; }
input[type="number"]::-webkit-inner-spin-button,
input[type="number"]::-webkit-outer-spin-button { -webkit-appearance: none; margin: 0; }
select { width: 150px; }
input[type="checkbox"] { width: 16px; height: 16px; margin: 0; accent-color: var(--_accent); }

/* Generic "pair" control (kept for schema flexibility; no current control uses
   it — figure size was removed, see docs/ux-design.md "Round five"): two
   labeled sub-rows, each its own slider + readout. */
.pair-rows { display: flex; flex-direction: column; gap: 2px; }
.pair-row { display: flex; align-items: center; gap: 4px; }
.pair-label { display: inline-block; width: 44px; color: var(--_muted); font-size: 11px; }

.revert { color: var(--_muted); border: 0; background: transparent; font-size: 13px; line-height: 1; padding: 1px 3px; }
.revert:hover { color: var(--_accent); }

/* Switch (styled checkbox), 28x16 */
input.switch {
  appearance: none; -webkit-appearance: none; position: relative;
  width: 28px; height: 16px; border-radius: 999px; padding: 0;
  background: var(--_border); border: 1px solid var(--_border); cursor: pointer; transition: background 0.1s;
}
input.switch::after {
  content: ""; position: absolute; top: 1px; left: 1px; width: 12px; height: 12px;
  border-radius: 50%; background: var(--_bg); transition: left 0.1s;
}
input.switch:checked { background: var(--_accent); border-color: var(--_accent); }
input.switch:checked::after { left: 13px; }

/* Segmented control (enum, <=4 options), 22px tall, segments >=28px */
.segmented { display: inline-flex; border: 1px solid var(--_border); border-radius: 6px; overflow: hidden; height: 22px; }
.segmented button {
  border: 0; border-radius: 0; background: var(--_field-bg); padding: 0 8px; font-size: 11.5px;
  min-width: 28px; border-right: 1px solid var(--_border); line-height: 20px;
}
.segmented button:last-child { border-right: 0; }
.segmented button[aria-pressed="true"] { background: var(--_accent); color: #fff; }

/* Range + readout (no separate number box) */
input[type="range"] { width: 96px; accent-color: var(--_accent); }
.readout { display: inline-block; min-width: 32px; text-align: right; font-variant-numeric: tabular-nums; color: var(--_muted); }

/* Color-cycle swatch list */
.swatch-list { display: inline-flex; flex-direction: row; flex-wrap: nowrap; gap: 4px; align-items: center; }
.swatch-list .preset { display: inline-flex; padding: 2px; border-radius: 4px; background: var(--_field-bg); }
.swatch-list .preset[aria-pressed="true"] { border-color: var(--_accent); box-shadow: 0 0 0 1px var(--_accent) inset; }
.swatches { display: inline-flex; }
.swatches i { display: inline-block; width: 8px; height: 8px; }

/* Per-line ("linecycle") table: a simple vertical stack — label row, table,
   "+ line" — with no dead space between them. The revert button lives in the
   label row (via .control-label-row), not below the table. */
.row[data-control="line_cycle"] { display: flex; flex-direction: column; gap: 4px; }
.row[data-control="line_cycle"] .badges { display: inline-flex; }
.row[data-control="line_cycle"] .control { display: block; }

.control-label-row { display: flex; align-items: center; gap: 4px; }
.control-label-row label { flex: 1; margin: 0; }
.control-label-row .revert { margin-left: auto; }

/* One shared CSS grid for the header and every row (not a grid per row), so
   the Width/Style headings line up with their columns exactly. */
.linecycle {
  display: grid; grid-template-columns: 14px 22px 48px max-content;
  column-gap: 6px; align-items: center; width: 100%;
}
.linecycle .line-row { display: contents; }
.linecycle .line-head span { color: var(--_muted); font-size: 11px; }
.linecycle .line-index { color: var(--_muted); font-size: 11px; text-align: right; }
input.line-color { width: 18px; height: 18px; padding: 0; border: 1px solid var(--_border); border-radius: 3px; background: none; }
input.line-width { width: 48px; font-size: 12px; }
.segmented.line-style { height: 20px; width: max-content; justify-self: start; }
.segmented.line-style button { min-width: 22px; padding: 0 4px; font-size: 11px; }
button.add-line {
  grid-column: 1 / -1; justify-self: start; margin-top: 2px; border: 0; background: transparent;
  color: var(--_muted); font-size: 11px; padding: 2px 2px;
}
button.add-line:hover { color: var(--_accent); }

/* Legend position: x/y sliders first, "Snap to" underneath (see
   docs/ux-design.md, "Round five"). The control itself is block, not the
   usual flex row, so the two pieces stack instead of sitting side by side. */
.row[data-control="legend_loc"] .control { display: block; }
.legend-xy { display: flex; flex-direction: column; gap: 2px; }
.legend-xy-row { display: flex; align-items: center; gap: 6px; }
.legend-xy-row .legend-label { display: inline-block; width: 10px; color: var(--_muted); font-size: 11px; }
.legend-snap { display: flex; align-items: center; gap: 6px; margin-top: 4px; }
.legend-snap .snap-label { color: var(--_muted); font-size: 11px; }
select.snap { width: 120px; }

/* Style preset: the note under it says the change needs a re-run; while one
   is pending (the rerunKeys set has "style") it is highlighted. */
p.next-run { grid-column: 1 / -1; margin: 2px 0 0; color: var(--_muted); font-size: 11px; }
p.next-run.pending { color: var(--_accent); font-weight: 700; }

/* --- Reset menu --- */
.menu {
  position: fixed; z-index: 2147483000; min-width: 160px; padding: 4px; border-radius: 8px;
  background: var(--_bg); border: 1px solid var(--_border); box-shadow: 0 6px 20px rgba(0, 0, 0, 0.18);
}
.menu-item {
  display: block; width: 100%; text-align: left; border: 0; background: transparent;
  padding: 5px 8px; border-radius: 5px; font-size: 12px;
}
.menu-item:hover { background: var(--_section-bg); border-color: transparent; }
.menu pre.code {
  margin: 4px 0 0; padding: 6px; overflow-x: auto; font: 11px/1.4 var(--_mono);
  background: var(--_section-bg); border: 1px solid var(--_border); border-radius: 6px; white-space: pre;
  max-width: 320px;
}

/* "All lines" masters while the per-line table holds differing values */
.row.mixed input[type="range"] { opacity: 0.55; }
.row.mixed .readout { color: var(--_muted); font-style: italic; }
.row.mixed .segmented { opacity: 0.7; }
`;
  const DEFAULT_FEATURES = { livePreview: true, showCode: true, groups: null };
  function parseLayoutAttr(value) {
    return value === "rail" ? "rail" : value === "float" ? "float" : "pill";
  }
  const APPLY_DEBOUNCE_MS = 60;
  const RAIL_WIDTH = 50;
  const FLOAT_INSET_PX = 8;
  const DRAG_THRESHOLD_PX = 4;
  const LINESTYLE_GLYPHS = { "-": "―", "--": "– –", "-.": "–·", ":": "···" };
  const LINESTYLE_LABELS = { "-": "Solid", "--": "Dashed", "-.": "Dash-dot", ":": "Dotted" };
  const LINESTYLE_VALUES = ["-", "--", "-.", ":"];
  const LEGEND_FALLBACK_XY = {
    best: [0.75, 0.75],
    "upper right": [0.75, 0.75],
    "upper left": [0.05, 0.75],
    "lower left": [0.05, 0.05],
    "lower right": [0.75, 0.05],
    right: [0.75, 0.45],
    "center right": [0.75, 0.45],
    "center left": [0.05, 0.45],
    "lower center": [0.4, 0.05],
    "upper center": [0.4, 0.75],
    center: [0.4, 0.45]
  };
  function schemaDefaults() {
    const rc = {};
    for (const c of CONTROLS) for (const k of c.keys) rc[k] = Array.isArray(c.default) ? [...c.default] : c.default;
    return rc;
  }
  function cloneSettings(s) {
    const rc = {};
    for (const [k, v] of Object.entries(s.rc)) rc[k] = Array.isArray(v) ? [...v] : v;
    return { style: s.style, rc };
  }
  function resizeArray(arr, len, fill) {
    const out = [];
    for (let i = 0; i < len; i++) out.push(i < arr.length ? arr[i] : fill);
    return out;
  }
  function el(tag, props = {}, ...children) {
    const node = document.createElement(tag);
    const { class: cls, ...rest } = props;
    if (cls) node.className = cls;
    Object.assign(node, rest);
    for (const c of children) node.append(c);
    return node;
  }
  function boolOn(spec) {
    return spec.onValue === void 0 ? true : spec.onValue;
  }
  function boolOff(spec) {
    return spec.offValue === void 0 ? false : spec.offValue;
  }
  class PlotpolishPanel extends HTMLElement {
    constructor() {
      super();
      this.hostRcKeys = [];
      this._backend = null;
      this.client = null;
      this._sink = null;
      this.unsubscribeSink = null;
      this._features = { ...DEFAULT_FEATURES };
      this.settings = defaultSettings();
      this.baseline = schemaDefaults();
      this.figureRc = {};
      this.figure = null;
      this.styles = ["default"];
      this.overridden = /* @__PURE__ */ new Set();
      this.unknownKeys = [];
      this.fenceError = null;
      this.stale = false;
      this.rerunKeys = /* @__PURE__ */ new Set();
      this.backendState = "none";
      this.backendMessage = "";
      this.writing = false;
      this.pendingApply = {};
      this.applyTimer = null;
      this.applyChain = Promise.resolve();
      this._open = false;
      this._category = null;
      this._menuOpen = false;
      this._codeOpen = false;
      this._layoutMode = "pill";
      this._layoutForced = false;
      this._settingLayoutAttr = false;
      this._figureElement = null;
      this.moreOpen = /* @__PURE__ */ new Map();
      this.dragPos = null;
      this.dragStart = null;
      this.pillPos = null;
      this.pillDragStart = null;
      this.resizeObserver = null;
      this.docClickHandler = null;
      this.views = /* @__PURE__ */ new Map();
      this.tabViews = /* @__PURE__ */ new Map();
      this.onWindowPointerEnd = (e) => {
        const id = e.pointerId;
        const matches = (startId) => id === void 0 || id === startId;
        if (this.pillDragStart && matches(this.pillDragStart.pointerId)) this.endPillDrag(id);
        if (this.dragStart && matches(this.dragStart.pointerId)) this.endHeaderDrag(id);
      };
      this.onWindowReflow = () => {
        this.measureLayout();
        this.positionRail();
        this.positionFloatPill();
        if (this._open && !this.dragPos) this.positionPopover();
        if (this._menuOpen) this.positionMenu();
      };
      this.extraLineRows = 0;
      this.root = this.attachShadow({ mode: "open" });
      this.build();
    }
    static get observedAttributes() {
      return ["layout"];
    }
    connectedCallback() {
      this.subscribeSink();
      this.loadFromSink();
      this.update();
      this.measureLayout();
      this.positionRail();
      this.positionFloatPill();
      this.docClickHandler = (e) => {
        if (!this._menuOpen) return;
        const path = typeof e.composedPath === "function" ? e.composedPath() : [];
        if (path.includes(this.ui.menu) || path.includes(this.ui.menuToggle)) return;
        this.closeMenu();
      };
      document.addEventListener("click", this.docClickHandler, true);
      this.setupResizeObservers();
      window.addEventListener("resize", this.onWindowReflow);
      window.addEventListener("scroll", this.onWindowReflow, true);
      window.addEventListener("pointerup", this.onWindowPointerEnd);
      window.addEventListener("pointercancel", this.onWindowPointerEnd);
    }
    disconnectedCallback() {
      this.unsubscribeSink?.();
      this.unsubscribeSink = null;
      if (this.docClickHandler) {
        document.removeEventListener("click", this.docClickHandler, true);
        this.docClickHandler = null;
      }
      this.teardownResizeObservers();
      window.removeEventListener("resize", this.onWindowReflow);
      window.removeEventListener("scroll", this.onWindowReflow, true);
      window.removeEventListener("pointerup", this.onWindowPointerEnd);
      window.removeEventListener("pointercancel", this.onWindowPointerEnd);
    }
    attributeChangedCallback(name, oldValue, newValue) {
      if (name !== "layout" || this._settingLayoutAttr || newValue === oldValue) return;
      if (newValue === null) {
        this._layoutForced = false;
        this.measureLayout();
        this.positionRail();
        this.positionFloatPill();
        return;
      }
      this._layoutForced = true;
      const mode = parseLayoutAttr(newValue);
      if (mode !== this._layoutMode) {
        this._layoutMode = mode;
        this.positionRail();
        this.positionFloatPill();
        this.update();
      }
    }
    // -------------------------------------------------------------------------
    // Public API
    // -------------------------------------------------------------------------
    get backend() {
      return this._backend;
    }
    set backend(backend) {
      this._backend = backend;
      this.client = backend ? new HelperClient(backend) : null;
      this.backendState = backend ? "connecting" : "none";
      this.backendMessage = "";
      if (!backend) this.figure = null;
      this.update();
      if (backend) void this.refresh();
    }
    get sink() {
      return this._sink;
    }
    set sink(sink) {
      this._sink = sink;
      this.subscribeSink();
      this.loadFromSink();
      this.update();
    }
    /**
     * (Re)attach the sink's change listener. Called both when the sink is set and
     * from connectedCallback, because disconnectedCallback drops the subscription
     * and a host may re-parent the element -- WebAgg rebuilds the figure's DOM on
     * every run, so a panel mounted in that subtree is moved routinely. Without
     * this, the panel stops noticing edits made outside it after the first move.
     */
    subscribeSink() {
      this.unsubscribeSink?.();
      this.unsubscribeSink = null;
      const sink = this._sink;
      if (!sink?.subscribe) return;
      this.unsubscribeSink = sink.subscribe(() => {
        if (!this.writing) {
          this.loadFromSink();
          this.update();
        }
      });
    }
    get features() {
      return { ...this._features, groups: this._features.groups ? [...this._features.groups] : null };
    }
    set features(partial) {
      this._features = { ...this._features, ...partial };
      this.update();
    }
    /** The host's figure container: bounds the rail's position and its ResizeObserver. */
    get figureElement() {
      return this._figureElement;
    }
    set figureElement(value) {
      this._figureElement = value;
      this.teardownResizeObservers();
      this.setupResizeObservers();
      this.measureLayout();
      this.positionRail();
      this.positionFloatPill();
      this.update();
    }
    /**
     * "pill", "rail" or "float" (the pill as a fixed layer over `figureElement`'s
     * top-right corner). Reflected as the `layout` attribute; set it to force one.
     */
    get layout() {
      return this._layoutMode;
    }
    set layout(value) {
      this._layoutForced = true;
      this.setLayoutMode(parseLayoutAttr(value));
    }
    /** Whether the popover is visible. Reflected as the `open` attribute. */
    get open() {
      return this._open;
    }
    set open(value) {
      const next = Boolean(value);
      if (next === this._open) return;
      if (next) this.showCategory(this._category ?? this.firstVisibleGroupId());
      else {
        this._open = false;
        this.reflectOpenAttr();
        this.update();
      }
    }
    /** The group id shown in the popover, or null when closed. Mutate via showCategory(). */
    get category() {
      return this._category;
    }
    /** Open the popover if closed (on the last/first category), or close it if open. */
    toggle() {
      this.open = !this._open;
    }
    /** Show `id`'s controls in the popover (opening it), or close the popover for null. */
    showCategory(id) {
      const wasOpen = this._open;
      const changed = this._category !== id;
      this._category = id;
      this._open = id !== null;
      this.reflectOpenAttr();
      this.update();
      if (this._open && (!wasOpen || changed)) {
        if (this.dragPos) this.applyDragPosition();
        else this.positionPopover();
      }
    }
    /** A copy of the current settings. */
    getSettings() {
      return cloneSettings(this.settings);
    }
    /** The block the current settings generate, or null when everything is default. */
    getBlock() {
      return generateBlock(this.settings);
    }
    get currentFenceError() {
      return this.fenceError;
    }
    get availableStyles() {
      return [...this.styles];
    }
    /** Effective value of an rc key: the user's setting, else the baseline from the backend/style. */
    effective(key) {
      return this.settings.rc[key] ?? this.baseline[key];
    }
    /**
     * Re-read the source and, when a backend is attached, the style list and the
     * live figure. Hosts call this after every run.
     */
    async refresh() {
      this.loadFromSink();
      this.stale = false;
      this.rerunKeys.clear();
      if (this.client) {
        try {
          const [styles, intro] = await Promise.all([this.client.listStyles(), this.client.introspect()]);
          this.styles = styles.length ? styles : ["default"];
          this.baseline = { ...schemaDefaults(), ...intro.rc };
          this.figureRc = { ...intro.rc };
          this.overridden = new Set(intro.overridden);
          this.figure = intro.figure;
          this.backendState = "ready";
          this.backendMessage = `matplotlib ${intro.matplotlib}${intro.figure ? "" : ", no figure yet"}`;
        } catch (e) {
          this.backendState = "error";
          this.backendMessage = e instanceof Error ? e.message : String(e);
          this.emitError(e, "refresh");
        }
      }
      this.update();
    }
    /** Clear every setting. Removes the block from the source. */
    reset() {
      const previous = this.settings;
      this.settings = defaultSettings();
      this.writeToSink();
      const keys = Object.keys(previous.rc);
      if (this.canPreview) {
        if (previous.style !== "default") this.applyStyle("default", keys);
        else this.scheduleApply(this.baselineFor(keys));
      }
      this.rerunKeys.clear();
      if (previous.style !== "default") this.noteRerun(["style"]);
      if (!this.canPreview && keys.length) this.noteRerun(keys);
      this.emitChange();
      this.update();
    }
    /** Replace a malformed fence wholesale with the current settings. Explicit user action only. */
    replaceBlock() {
      const sink = this._sink;
      if (!sink) return;
      const src = sink.getSource();
      if (src === null) return;
      this.writing = true;
      try {
        sink.setSource(replaceFence(src, this.settings));
      } finally {
        this.writing = false;
      }
      this.loadFromSink();
      this.emitChange();
      this.update();
    }
    /** Resolves when pending live-apply / set_style work has finished. For tests and hosts. */
    async settle() {
      for (let i = 0; i < 10; i++) {
        await this.applyChain;
        if (!this.applyTimer) break;
        await new Promise((r) => setTimeout(r, APPLY_DEBOUNCE_MS + 5));
      }
      await this.applyChain;
    }
    // -------------------------------------------------------------------------
    // Source handling
    // -------------------------------------------------------------------------
    loadFromSink() {
      const sink = this._sink;
      if (!sink) return;
      const src = sink.getSource();
      if (src === null) return;
      try {
        const parsed = parseBlock(src);
        this.fenceError = null;
        this.settings = parsed ? parsed.settings : defaultSettings();
        this.unknownKeys = parsed ? parsed.unknownKeys : [];
      } catch (e) {
        if (e instanceof FenceError) {
          this.fenceError = e;
        } else {
          throw e;
        }
      }
    }
    writeToSink() {
      const sink = this._sink;
      if (!sink) return null;
      const src = sink.getSource();
      this.writing = true;
      try {
        if (src === null) {
          const block = generateBlock(this.settings) ?? "";
          sink.setSource(block);
          return null;
        }
        try {
          const next = upsertBlock(src, this.settings);
          this.fenceError = null;
          if (next !== src) sink.setSource(next);
          return next;
        } catch (e) {
          if (e instanceof FenceError) {
            this.fenceError = e;
            this.emitError(e, "write");
            return src;
          }
          throw e;
        }
      } finally {
        this.writing = false;
      }
    }
    // -------------------------------------------------------------------------
    // State changes from controls
    // -------------------------------------------------------------------------
    setKeys(spec, value) {
      const wasDefault = isDefaultSettings(this.settings);
      const apply = {};
      for (const key of spec.keys) {
        if (value === void 0) {
          delete this.settings.rc[key];
          const base = this.baseline[key];
          if (base !== void 0) apply[key] = base;
        } else {
          this.settings.rc[key] = Array.isArray(value) ? [...value] : value;
          apply[key] = this.settings.rc[key];
        }
      }
      if (value !== void 0) this.unifyPerLine(spec, apply);
      const seeded = this.seedPanelDefaults(wasDefault);
      this.writeToSink();
      if (spec.category === "rerun" || !this.canPreview) this.noteRerun(spec.keys);
      else this.scheduleApply(apply);
      this.applySeeded(seeded);
      this.emitChange();
      this.update();
    }
    /**
     * "Line width (all)" and "Line style (all)" are masters over the per-line
     * table. Setting one drops the matching per-line array from
     * axes.prop_cycle, so every row follows it and the block stays short, and
     * previews the new value on every line (the lines currently sit at their
     * per-line values, so the preview carries an explicit array of the master).
     */
    unifyPerLine(spec, apply) {
      const prop = spec.keys.includes("lines.linewidth") ? "linewidth" : spec.keys.includes("lines.linestyle") ? "linestyle" : null;
      if (!prop) return;
      const cycle = this.settings.rc["axes.prop_cycle"];
      if (!isPropCycle(cycle) || !cycle[prop]) return;
      const n = cycle[prop].length;
      const rest = { color: [...cycle.color] };
      if (prop !== "linewidth" && cycle.linewidth) rest.linewidth = [...cycle.linewidth];
      if (prop !== "linestyle" && cycle.linestyle) rest.linestyle = [...cycle.linestyle];
      this.settings.rc["axes.prop_cycle"] = rest.linewidth || rest.linestyle ? rest : [...rest.color];
      const master = this.settings.rc[spec.keys[0]];
      const preview = { color: [...rest.color] };
      if (rest.linewidth) preview.linewidth = [...rest.linewidth];
      if (rest.linestyle) preview.linestyle = [...rest.linestyle];
      if (prop === "linewidth" && typeof master === "number") preview.linewidth = Array(n).fill(master);
      if (prop === "linestyle" && typeof master === "string") preview.linestyle = Array(n).fill(master);
      apply["axes.prop_cycle"] = preview;
    }
    /**
     * The per-line values for `prop` across the rows the table actually shows,
     * or null when the cycle carries none.
     *
     * The array spans the palette (up to ten entries) while the table shows one
     * row per line in the figure, so the tail belongs to rows the user can
     * neither see nor edit. Reading it reported "mixed" for two rows that both
     * plainly said 8.
     */
    perLineVisible(prop) {
      const cycle = this.effective("axes.prop_cycle");
      if (!isPropCycle(cycle)) return null;
      const arr = cycle[prop];
      if (!arr || !arr.length) return null;
      const cycleSpec = CONTROLS.find((c) => c.type === "linecycle");
      const visible = cycleSpec ? this.lineCycleRowCount(cycleSpec) : arr.length;
      return arr.slice(0, Math.min(arr.length, Math.max(1, visible)));
    }
    /** True when the rows the table shows carry per-line values that differ. */
    perLineMixed(prop) {
      const shown = this.perLineVisible(prop);
      return !!shown && shown.length > 1 && shown.some((v) => v !== shown[0]);
    }
    /**
     * The value every shown row shares, or null when they differ or the cycle
     * carries no per-line value. The "(all)" master displays this in preference
     * to its own scalar: with every row reading 8, a master reading 1.5 (the
     * unset scalar's default) contradicts the table right above it.
     */
    perLineUniform(prop) {
      const shown = this.perLineVisible(prop);
      if (!shown || !shown.length) return null;
      return shown.some((v) => v !== shown[0]) ? null : shown[0];
    }
    setStyle(name) {
      if (name === this.settings.style) return;
      const wasDefault = isDefaultSettings(this.settings);
      this.settings.style = name;
      this.seedPanelDefaults(wasDefault);
      this.writeToSink();
      this.noteRerun(["style"]);
      if (this.client) this.applyStyle(name, []);
      this.emitChange();
      this.update();
    }
    /**
     * When settings go from fully default to non-default (and only then), set
     * every panelDefault control's keys to their panelDefault, provided none of
     * that control's keys are already set (e.g. by the very change that just
     * made settings non-default). Returns the keys that were seeded.
     */
    seedPanelDefaults(wasDefault) {
      if (!wasDefault || isDefaultSettings(this.settings)) return [];
      const seeded = [];
      for (const c of CONTROLS) {
        const def = c.panelDefault;
        if (def === void 0) continue;
        if (c.keys.some((k) => k in this.settings.rc)) continue;
        for (const key of c.keys) {
          this.settings.rc[key] = Array.isArray(def) ? [...def] : def;
          seeded.push(key);
        }
      }
      return seeded;
    }
    /** Live-apply (or note-rerun) keys seedPanelDefaults just added, alongside whatever triggered the change. */
    applySeeded(seeded) {
      if (!seeded.length) return;
      const rerunKeys = [];
      const apply = {};
      for (const key of seeded) {
        const ctrl = CONTROL_FOR_KEY.get(key);
        if (ctrl?.category === "rerun" || !this.canPreview) rerunKeys.push(key);
        else apply[key] = this.settings.rc[key];
      }
      if (rerunKeys.length) this.noteRerun(rerunKeys);
      if (Object.keys(apply).length && this.canPreview) this.scheduleApply(apply);
    }
    /**
     * Whether a change can be shown on the figure now, or only after a re-run.
     * False when the host attached no backend at all -- Trinket's Web Worker path
     * runs the program off the main thread, so there is nothing here to preview
     * against -- or when the host turned live preview off explicitly.
     */
    get canPreview() {
      return !!this.client && this._features.livePreview;
    }
    /** Baseline values for `keys` (what the figure should return to when an override is cleared). */
    baselineFor(keys) {
      const out = {};
      for (const key of keys) {
        const base = this.baseline[key];
        if (base !== void 0) out[key] = base;
      }
      return out;
    }
    /** Revert every control in `groupId` (and, for "look", the style preset). */
    resetCategory(groupId) {
      const specs = controlsInGroup(groupId);
      const keys = specs.flatMap((s) => s.keys).filter((k) => k in this.settings.rc);
      const willResetStyle = groupId === "look" && this.settings.style !== "default" && this.settings.style !== "";
      for (const key of keys) delete this.settings.rc[key];
      if (willResetStyle) this.settings.style = "default";
      this.writeToSink();
      if (this.canPreview) {
        if (willResetStyle) this.applyStyle("default", keys);
        else if (keys.length) this.scheduleApply(this.baselineFor(keys));
      }
      if (willResetStyle) this.noteRerun(["style"]);
      if (!this.canPreview && keys.length) this.noteRerun(keys);
      this.emitChange();
      this.update();
    }
    /**
     * set_style in the session (queued behind pending live applies so
     * `settle()` covers it), refresh the baseline, then re-apply the current
     * overrides on top — plus the baseline for `restoreKeys` that were just cleared.
     */
    applyStyle(name, restoreKeys) {
      const client = this.client;
      if (!client) return;
      this.stale = true;
      this.applyChain = this.applyChain.then(() => client.setStyle(name, this.hostRcKeys)).then((rc) => {
        this.baseline = { ...schemaDefaults(), ...rc };
        if (this._features.livePreview) {
          const again = { ...this.baselineFor(restoreKeys), ...this.settings.rc };
          if (Object.keys(again).length) this.scheduleApply(again);
        }
        this.update();
      }).catch((e) => {
        this.backendState = "error";
        this.backendMessage = e instanceof Error ? e.message : String(e);
        this.emitError(e, "set_style");
        this.update();
      });
    }
    scheduleApply(rc) {
      Object.assign(this.pendingApply, rc);
      if (this.applyTimer) clearTimeout(this.applyTimer);
      this.applyTimer = setTimeout(() => {
        this.applyTimer = null;
        const batch = this.pendingApply;
        this.pendingApply = {};
        if (!Object.keys(batch).length || !this.client) return;
        const client = this.client;
        const previous = {};
        for (const key of Object.keys(batch)) {
          const seen = this.figureRc[key];
          if (seen !== void 0) previous[key] = seen;
        }
        const legendLocIsNamed = typeof batch["legend.loc"] === "string";
        this.applyChain = this.applyChain.then(() => client.applyLive(batch, true, previous)).then((result) => {
          for (const key of result.applied) {
            const value = batch[key];
            if (value !== void 0) this.figureRc[key] = value;
          }
          if (result.deferred.length) this.noteRerun(result.deferred);
          if (this.backendState === "error") {
            this.backendState = "ready";
            this.backendMessage = "";
          }
          this.update();
          if (legendLocIsNamed) {
            return client.introspect().then((intro) => {
              this.figure = intro.figure;
              this.update();
            });
          }
          return void 0;
        }).catch((e) => {
          this.backendState = "error";
          this.backendMessage = e instanceof BackendError ? e.message : String(e);
          this.emitError(e, "apply_live");
          this.update();
        });
      }, APPLY_DEBOUNCE_MS);
    }
    noteRerun(keys) {
      for (const k of keys) this.rerunKeys.add(k);
      this.emit("rerun-needed", { keys: [...this.rerunKeys], style: this.settings.style });
    }
    emitChange() {
      const sink = this._sink;
      const source = sink ? sink.getSource() : null;
      this.emit("change", { settings: cloneSettings(this.settings), block: generateBlock(this.settings), source });
    }
    emitError(error, context) {
      const err = error instanceof Error ? error : new Error(String(error));
      this.emit("error", { error: err, context });
    }
    emit(name, detail) {
      this.dispatchEvent(new CustomEvent(`${EVENT_PREFIX}-${name}`, { detail, bubbles: true, composed: true }));
    }
    // -------------------------------------------------------------------------
    // Navigation: tabs, popover, menu, layout
    // -------------------------------------------------------------------------
    firstVisibleGroupId() {
      for (const g of GROUPS) if (this.isGroupVisible(g)) return g.id;
      return null;
    }
    handleTabClick(id) {
      if (this._open && this._category === id) this.showCategory(null);
      else this.showCategory(id);
    }
    reflectOpenAttr() {
      this.toggleAttribute("open", this._open);
    }
    handleEscape() {
      if (this._menuOpen) {
        this.closeMenu();
        return;
      }
      if (this._open) this.showCategory(null);
    }
    activeTabButton(tabView) {
      const idx = this._layoutMode === "rail" ? 1 : 0;
      return tabView.buttons[idx] ?? tabView.buttons[0];
    }
    positionPopover() {
      if (!this._category) return;
      const tabView = this.tabViews.get(this._category);
      const tab = tabView && this.activeTabButton(tabView);
      if (!tab) return;
      const { popover, caret } = this.ui;
      const rect = tab.getBoundingClientRect();
      const popRect = popover.getBoundingClientRect();
      const width = popRect.width || 260;
      const height = popRect.height || 96;
      const vw = window.innerWidth || 0;
      const vh = window.innerHeight || 0;
      let left = rect.left;
      const roomAbove = rect.top - 6 - height >= 4;
      const below = this._layoutMode === "float" || !roomAbove;
      let top = below ? rect.bottom + 6 : rect.top - 6 - height;
      if (vw > 0) left = Math.max(4, Math.min(left, vw - width - 4));
      if (vh > 0) top = Math.max(4, Math.min(top, vh - height - 4));
      popover.style.left = `${left}px`;
      popover.style.top = `${top}px`;
      popover.classList.toggle("below", below);
      caret.hidden = false;
      const center = rect.left + rect.width / 2 - left;
      caret.style.left = `${Math.max(8, Math.min(center, Math.max(8, width - 16)))}px`;
    }
    applyDragPosition() {
      if (!this.dragPos) return;
      this.ui.popover.style.left = `${this.dragPos.x}px`;
      this.ui.popover.style.top = `${this.dragPos.y}px`;
      this.ui.caret.hidden = true;
    }
    reanchor() {
      this.dragPos = null;
      this.ui.popover.classList.remove("dragging");
      this.positionPopover();
    }
    onHeaderPointerDown(e) {
      const target = e.target;
      if (target?.closest("button")) return;
      const rect = this.ui.popover.getBoundingClientRect();
      this.dragStart = {
        x: e.clientX ?? 0,
        y: e.clientY ?? 0,
        left: rect.left,
        top: rect.top,
        pointerId: e.pointerId,
        captured: false
      };
    }
    onHeaderPointerMove(e) {
      const start = this.dragStart;
      if (!start) return;
      if (e.buttons === 0) {
        this.endHeaderDrag();
        return;
      }
      const dx = (e.clientX ?? 0) - start.x;
      const dy = (e.clientY ?? 0) - start.y;
      if (!start.captured) {
        if (Math.hypot(dx, dy) < DRAG_THRESHOLD_PX) return;
        start.captured = true;
        try {
          this.ui.header.setPointerCapture?.(start.pointerId);
        } catch {
        }
      }
      let left = start.left + dx;
      let top = start.top + dy;
      const headerRect = this.ui.header.getBoundingClientRect();
      const hw = headerRect.width || 200;
      const vw = window.innerWidth || 0;
      const vh = window.innerHeight || 0;
      if (vw > 0) left = Math.max(40 - hw, Math.min(left, vw - 40));
      if (vh > 0) top = Math.max(0, Math.min(top, vh - 40));
      this.dragPos = { x: left, y: top };
      this.ui.popover.classList.add("dragging");
      this.ui.popover.style.left = `${left}px`;
      this.ui.popover.style.top = `${top}px`;
      this.ui.caret.hidden = true;
    }
    onHeaderPointerUp(e) {
      this.endHeaderDrag(e.pointerId);
    }
    /**
     * End a popover drag from any exit path: a pointerup on the header, one that
     * landed anywhere else, a cancelled gesture, or a move that arrives with no
     * button held. Leaving `dragStart` set is what let a later hover silently
     * resume the drag without a click.
     */
    endHeaderDrag(pointerId) {
      const start = this.dragStart;
      if (!start) return;
      if (start.captured) {
        try {
          this.ui.header.releasePointerCapture?.(pointerId ?? start.pointerId);
        } catch {
        }
        this.ui.popover.classList.remove("dragging");
      }
      this.dragStart = null;
    }
    toggleMore(groupId) {
      this.moreOpen.set(groupId, !(this.moreOpen.get(groupId) ?? false));
      this.update();
    }
    toggleMenu() {
      this._menuOpen = !this._menuOpen;
      this.update();
      if (this._menuOpen) this.positionMenu();
    }
    closeMenu() {
      if (!this._menuOpen) return;
      this._menuOpen = false;
      this.update();
    }
    positionMenu() {
      const rect = this.ui.menuToggle.getBoundingClientRect();
      this.ui.menu.style.left = `${rect.left}px`;
      this.ui.menu.style.top = `${rect.bottom + 4}px`;
    }
    setupResizeObservers() {
      if (typeof ResizeObserver === "undefined") return;
      this.resizeObserver = new ResizeObserver(() => this.onWindowReflow());
      if (this.parentElement) this.resizeObserver.observe(this.parentElement);
      if (this._figureElement) this.resizeObserver.observe(this._figureElement);
    }
    teardownResizeObservers() {
      this.resizeObserver?.disconnect();
      this.resizeObserver = null;
    }
    /**
     * Auto-detect the layout (unless the host has forced `layout`): "float" is
     * the default once `figureElement` is set and has a real size; with no
     * figure element, or one with zero size (not yet laid out), it falls back
     * to the inline "pill". Auto "rail" is no longer chosen: floating over the
     * figure replaces the old too-narrow-toolbar fallback, which "rail" now
     * serves only as a host-forced option.
     */
    measureLayout() {
      if (this._layoutForced) return;
      if (!this._figureElement) {
        this.setLayoutMode("pill");
        return;
      }
      const rect = this._figureElement.getBoundingClientRect();
      this.setLayoutMode(rect.width > 0 && rect.height > 0 ? "float" : "pill");
    }
    setLayoutMode(mode) {
      if (this._layoutMode === mode) {
        this.reflectLayoutAttr();
        return;
      }
      this._layoutMode = mode;
      this.reflectLayoutAttr();
      this.positionRail();
      this.positionFloatPill();
      this.update();
    }
    reflectLayoutAttr() {
      this._settingLayoutAttr = true;
      this.setAttribute("layout", this._layoutMode);
      this._settingLayoutAttr = false;
    }
    positionRail() {
      const rail = this.ui?.rail;
      if (!rail || this._layoutMode !== "rail" || !this._figureElement) return;
      const rect = this._figureElement.getBoundingClientRect();
      rail.style.position = "fixed";
      rail.style.top = `${rect.top}px`;
      rail.style.left = `${Math.max(0, rect.right - RAIL_WIDTH)}px`;
      rail.style.width = `${RAIL_WIDTH}px`;
    }
    /**
     * Position the pill as a fixed layer 8px inset from `figureElement`'s
     * bounding rect (top-right corner). With no figure element the inset is
     * measured from the viewport, so a host-forced "float" still renders
     * sensibly. While the pill has been dragged (`pillPos` set), that position
     * wins instead and reflows (resize/scroll) must not snap it back.
     */
    positionFloatPill() {
      const pill = this.ui?.pill;
      if (!pill) return;
      if (this.pillPos) {
        pill.style.left = `${this.pillPos.x}px`;
        pill.style.top = `${this.pillPos.y}px`;
        pill.style.right = "";
        return;
      }
      if (this._layoutMode !== "float") {
        pill.style.top = "";
        pill.style.right = "";
        return;
      }
      const rect = this._figureElement?.getBoundingClientRect();
      const vw = window.innerWidth || 0;
      const top = (rect?.top ?? 0) + FLOAT_INSET_PX;
      const right = Math.max(0, vw - (rect?.right ?? vw)) + FLOAT_INSET_PX;
      pill.style.top = `${top}px`;
      pill.style.right = `${right}px`;
    }
    onPillGripPointerDown(e) {
      const target = e.target;
      if (target?.closest("button")) return;
      const rect = this.ui.pill.getBoundingClientRect();
      this.pillDragStart = {
        x: e.clientX ?? 0,
        y: e.clientY ?? 0,
        left: rect.left,
        top: rect.top,
        pointerId: e.pointerId,
        captured: false
      };
    }
    onPillGripPointerMove(e) {
      const start = this.pillDragStart;
      if (!start) return;
      if (e.buttons === 0) {
        this.endPillDrag();
        return;
      }
      const dx = (e.clientX ?? 0) - start.x;
      const dy = (e.clientY ?? 0) - start.y;
      if (!start.captured) {
        if (Math.hypot(dx, dy) < DRAG_THRESHOLD_PX) return;
        start.captured = true;
        try {
          this.ui.pillGrip.setPointerCapture?.(start.pointerId);
        } catch {
        }
        this.ui.pill.style.position = "fixed";
        this.ui.pill.style.right = "";
      }
      let left = start.left + dx;
      let top = start.top + dy;
      const pillRect = this.ui.pill.getBoundingClientRect();
      const pw = pillRect.width || 100;
      const ph = pillRect.height || 26;
      const vw = window.innerWidth || 0;
      const vh = window.innerHeight || 0;
      if (vw > 0) left = Math.max(40 - pw, Math.min(left, vw - 40));
      if (vh > 0) top = Math.max(40 - ph, Math.min(top, vh - 40));
      this.pillPos = { x: left, y: top };
      this.ui.pill.classList.add("dragging");
      this.ui.pill.style.left = `${left}px`;
      this.ui.pill.style.top = `${top}px`;
      if (this._open && !this.dragPos) this.positionPopover();
    }
    onPillGripPointerUp(e) {
      this.endPillDrag(e.pointerId);
    }
    /** End a pill drag from any exit path. See endHeaderDrag. */
    endPillDrag(pointerId) {
      const start = this.pillDragStart;
      if (!start) return;
      if (start.captured) {
        try {
          this.ui.pillGrip.releasePointerCapture?.(pointerId ?? start.pointerId);
        } catch {
        }
        this.ui.pill.classList.remove("dragging");
      }
      this.pillDragStart = null;
    }
    /** Double-clicking the pill's grip: drop `pillPos` and re-anchor to the figure's top-right (or back inline). */
    reanchorPill() {
      this.pillPos = null;
      this.ui.pill.classList.remove("dragging");
      this.ui.pill.style.position = "";
      this.ui.pill.style.left = "";
      this.positionFloatPill();
      if (this._open && !this.dragPos) this.positionPopover();
    }
    isGroupVisible(group) {
      if (this._features.groups !== null && !this._features.groups.includes(group.id)) return false;
      if (group.hideWhen === "no-legend" && this.noLegendOnFigure()) {
        return controlsInGroup(group.id).some((c) => c.keys.some((k) => k in this.settings.rc));
      }
      return true;
    }
    noLegendOnFigure() {
      return this.backendState === "ready" && this.figure !== null && !this.figure.axes.some((a) => a.legend !== null);
    }
    /**
     * Where to put the x/y sliders while `legend.loc` is a named string: the
     * lower-left corner matplotlib actually drew the legend at, from the last
     * introspection (first axes with a legend and a known `xy`), or else a
     * representative corner for that name.
     */
    legendXyForName(name) {
      const axesWithXy = this.figure?.axes.find((a) => a.legend !== null && a.legend.xy);
      const xy = axesWithXy?.legend?.xy;
      if (xy) return xy;
      return LEGEND_FALLBACK_XY[name] ?? LEGEND_FALLBACK_XY.center;
    }
    legendNoteNeeded(group) {
      if (group.hideWhen !== "no-legend") return false;
      if (!this.noLegendOnFigure()) return false;
      return controlsInGroup(group.id).some((c) => c.keys.some((k) => k in this.settings.rc));
    }
    groupHasChanges(groupId) {
      if (groupId === "look" && this.settings.style !== "" && this.settings.style !== "default") return true;
      return controlsInGroup(groupId).some((c) => c.keys.some((k) => k in this.settings.rc));
    }
    groupHasRerunPending(groupId) {
      if (groupId === "look" && this.rerunKeys.has("style")) return true;
      return controlsInGroup(groupId).some((c) => c.keys.some((k) => this.rerunKeys.has(k)));
    }
    // -------------------------------------------------------------------------
    // DOM: build once
    // -------------------------------------------------------------------------
    buildTabButton(group) {
      const dot = el("span", { class: "dot", hidden: true });
      const rerun = el("span", { class: "rerun", hidden: true }, "↻");
      const btn = el("button", { type: "button", class: "tab", title: group.help ?? "" }, group.label, dot, rerun);
      btn.dataset.group = group.id;
      btn.setAttribute("role", "tab");
      btn.setAttribute("aria-selected", "false");
      btn.addEventListener("click", () => this.handleTabClick(group.id));
      return { btn, dot, rerun };
    }
    build() {
      const style = el("style");
      style.textContent = css;
      const errMark = el("span", { class: "err", hidden: true, title: "There is a problem with the generated block." }, "!");
      const menuToggle = el("button", { type: "button", class: "menu-toggle", title: "Reset menu" }, "↺ ▾");
      menuToggle.setAttribute("aria-haspopup", "menu");
      menuToggle.addEventListener("click", () => this.toggleMenu());
      const pillTabs = [];
      const railTabs = [];
      for (const group of GROUPS) {
        const pillTab = this.buildTabButton(group);
        const railTab = this.buildTabButton(group);
        this.tabViews.set(group.id, {
          buttons: [pillTab.btn, railTab.btn],
          dots: [pillTab.dot, railTab.dot],
          reruns: [pillTab.rerun, railTab.rerun]
        });
        pillTabs.push(pillTab.btn);
        railTabs.push(railTab.btn);
      }
      const pillGrip = el("span", { class: "grip", title: "Drag to move" }, "⋮⋮");
      pillGrip.addEventListener("pointerdown", (e) => this.onPillGripPointerDown(e));
      pillGrip.addEventListener("pointermove", (e) => this.onPillGripPointerMove(e));
      pillGrip.addEventListener("pointerup", (e) => this.onPillGripPointerUp(e));
      pillGrip.addEventListener("pointercancel", (e) => this.endPillDrag(e.pointerId));
      pillGrip.addEventListener("dblclick", () => this.reanchorPill());
      const pill = el("div", { class: "pill", role: "tablist" }, pillGrip, ...pillTabs, errMark, menuToggle);
      const rail = el("div", { class: "rail", role: "tablist", hidden: true }, ...railTabs);
      const grip = el("span", { class: "grip" }, "⋮⋮");
      const title = el("span", { class: "title" });
      const reanchorBtn = el("button", { type: "button", class: "reanchor", title: "Move back to the tab" }, "⌖");
      const closeBtn = el("button", { type: "button", class: "close" }, "✕");
      closeBtn.setAttribute("aria-label", "Close");
      const header = el("div", { class: "pop-head", title: "Drag to move" }, grip, title, reanchorBtn, closeBtn);
      header.addEventListener("pointerdown", (e) => this.onHeaderPointerDown(e));
      header.addEventListener("pointermove", (e) => this.onHeaderPointerMove(e));
      header.addEventListener("pointerup", (e) => this.onHeaderPointerUp(e));
      header.addEventListener("pointercancel", (e) => this.endHeaderDrag(e.pointerId));
      header.addEventListener("dblclick", () => this.reanchor());
      reanchorBtn.addEventListener("click", () => this.reanchor());
      closeBtn.addEventListener("click", () => this.showCategory(null));
      const caret = el("div", { class: "caret" });
      const fenceMessage = el("span");
      const replaceButton = el("button", { type: "button", class: "small" }, "Replace block");
      replaceButton.addEventListener("click", () => this.replaceBlock());
      const banner = el("div", { class: "banner error", role: "alert", hidden: true }, fenceMessage, replaceButton);
      const unknownNote = el("p", { class: "unknown-note muted", hidden: true });
      const groupViews = /* @__PURE__ */ new Map();
      const groupEls = [];
      for (const group of GROUPS) {
        const note = el("p", { class: "note", hidden: true });
        const primaryRows = el("div", { class: "rows primary" });
        this.appendRows(primaryRows, group, controlsInGroup(group.id, "primary"), false);
        const children = [note, primaryRows];
        const moreSpecs = controlsInGroup(group.id, "more");
        let moreButton;
        let moreRows;
        if (moreSpecs.length) {
          moreRows = el("div", { class: "rows more", hidden: true });
          this.appendRows(moreRows, group, moreSpecs, true);
          moreButton = el("button", { type: "button", class: "more" }, "More ▸");
          moreButton.addEventListener("click", () => this.toggleMore(group.id));
          children.push(moreButton, moreRows);
        }
        const container = el("div", { class: "group", hidden: true }, ...children);
        container.dataset.group = group.id;
        groupViews.set(group.id, { container, note, primaryRows, moreRows, moreButton });
        groupEls.push(container);
      }
      const popBody = el("div", { class: "pop-body" }, banner, ...groupEls, unknownNote);
      const popover = el("div", { class: "popover", role: "dialog", hidden: true }, header, caret, popBody);
      const resetCategoryItem = el("button", { type: "button", class: "menu-item", role: "menuitem" });
      resetCategoryItem.dataset.action = "reset-category";
      resetCategoryItem.addEventListener("click", () => {
        if (this._category) this.resetCategory(this._category);
        this.closeMenu();
      });
      const resetAllItem = el("button", { type: "button", class: "menu-item", role: "menuitem" }, "Reset all");
      resetAllItem.dataset.action = "reset-all";
      resetAllItem.addEventListener("click", () => {
        this.reset();
        this.closeMenu();
      });
      const showCodeItem = el("button", { type: "button", class: "menu-item", role: "menuitem" }, "Show code");
      showCodeItem.dataset.action = "show-code";
      const codePre = el("pre", { class: "code", hidden: true });
      showCodeItem.addEventListener("click", () => {
        this._codeOpen = !this._codeOpen;
        this.update();
      });
      const menu = el("div", { class: "menu", role: "menu", hidden: true }, resetCategoryItem, resetAllItem, showCodeItem, codePre);
      this.addEventListener("keydown", (e) => {
        if (e.key === "Escape") this.handleEscape();
      });
      this.root.append(style, pill, rail, popover, menu);
      this.ui = {
        pill,
        pillGrip,
        errMark,
        menuToggle,
        rail,
        popover,
        header,
        title,
        reanchorBtn,
        closeBtn,
        caret,
        popBody,
        banner,
        fenceMessage,
        unknownNote,
        menu,
        resetCategoryItem,
        resetAllItem,
        showCodeItem,
        codePre,
        groups: groupViews
      };
    }
    /** Append `specs`' rows into `container`; with `withHeadings`, insert a `.subhead` before each subgroup's first row. */
    appendRows(container, group, specs, withHeadings) {
      let lastSubgroup;
      for (const spec of specs) {
        if (withHeadings && group.subgroups && spec.subgroup && spec.subgroup !== lastSubgroup) {
          const sg = group.subgroups.find((s) => s.id === spec.subgroup);
          if (sg) container.append(el("p", { class: "subhead" }, sg.label));
          lastSubgroup = spec.subgroup;
        }
        container.append(this.buildControl(spec));
      }
    }
    buildControl(spec) {
      const id = `ctl-${spec.id}`;
      const label = el("label", { htmlFor: id, title: spec.help ?? "" }, spec.label);
      const badges = el("div", { class: "badges" });
      const control = el("div", { class: "control" });
      const revert = el("button", { type: "button", class: "revert", title: "Back to the style's default", hidden: true }, "↺");
      revert.setAttribute("aria-label", `Revert ${spec.label}`);
      revert.addEventListener("click", () => this.setKeys(spec, void 0));
      const row = spec.type === "linecycle" ? el("div", { class: "row" }, el("div", { class: "control-label-row" }, label, badges, revert), control) : el("div", { class: "row" }, label, badges, control);
      row.dataset.control = spec.id;
      if (spec.category === "save") row.title = "Applies when the figure is saved, not on screen.";
      const inputs = [];
      let segmented;
      let swatchList;
      let legendLoc;
      let rangeInput;
      let readout;
      let pairOuts;
      let lineRowsView;
      let addLineBtnView;
      let nextRunNote;
      const number = (extra = {}) => {
        const input = el("input", { type: "number", id, ...extra });
        if (spec.min !== void 0) input.min = String(spec.min);
        if (spec.max !== void 0) input.max = String(spec.max);
        if (spec.step !== void 0) input.step = String(spec.step);
        return input;
      };
      switch (spec.type) {
        case "style": {
          const select = el("select", { id });
          select.addEventListener("change", () => this.setStyle(select.value));
          inputs.push(select);
          control.append(select);
          const note = el("p", { class: "next-run" }, "Applies on the next run.");
          row.append(note);
          nextRunNote = note;
          break;
        }
        case "copycode": {
          const button = el("button", { type: "button", id, class: "copy-code" }, spec.label);
          const said = el("span", { class: "copy-said", hidden: true });
          button.addEventListener("click", () => {
            const block = this.getBlock();
            if (!block) {
              said.textContent = "Nothing to copy yet";
              said.hidden = false;
              window.setTimeout(() => {
                said.hidden = true;
              }, 2e3);
              return;
            }
            const done = (ok) => {
              said.textContent = ok ? "Copied" : "Copy failed — use Show code";
              said.hidden = false;
              window.setTimeout(() => {
                said.hidden = true;
              }, 2e3);
            };
            const clipboard = typeof navigator !== "undefined" ? navigator.clipboard : void 0;
            if (!clipboard?.writeText) {
              done(false);
              return;
            }
            clipboard.writeText(block).then(() => done(true), () => done(false));
          });
          control.append(button, said);
          break;
        }
        case "bool": {
          const input = el("input", { type: "checkbox", id, class: "switch" });
          input.addEventListener(
            "change",
            () => this.setKeys(spec, input.checked ? boolOn(spec) : boolOff(spec))
          );
          inputs.push(input);
          control.append(input);
          break;
        }
        case "number": {
          if (spec.min !== void 0 && spec.max !== void 0) {
            const range = el("input", { type: "range", id });
            range.min = String(spec.min);
            range.max = String(spec.max);
            if (spec.step !== void 0) range.step = String(spec.step);
            const out = el("span", { class: "readout" });
            range.addEventListener("input", () => {
              out.textContent = range.value;
              const n = Number(range.value);
              if (Number.isFinite(n)) this.setKeys(spec, n);
            });
            rangeInput = range;
            readout = out;
            inputs.push(range);
            control.append(range, out);
          } else {
            const input = number({ id });
            const commit = () => {
              const n = Number(input.value);
              if (input.value !== "" && Number.isFinite(n)) this.setKeys(spec, n);
              else this.update();
            };
            input.addEventListener("input", commit);
            input.addEventListener("change", commit);
            input.addEventListener("blur", () => this.update());
            inputs.push(input);
            control.append(input);
          }
          break;
        }
        case "fontsize": {
          const range = el("input", { type: "range", id, min: "6", max: "40", step: "0.5" });
          const out = el("span", { class: "readout" });
          const commit = () => {
            const n = Number(range.value);
            if (Number.isFinite(n)) this.setKeys(spec, n);
          };
          range.addEventListener("input", commit);
          range.addEventListener("change", commit);
          rangeInput = range;
          readout = out;
          inputs.push(range);
          control.append(range, out);
          break;
        }
        case "dpi": {
          const range = el("input", { type: "range", id, min: "72", max: "600", step: "1" });
          const out = el("span", { class: "readout" });
          const commit = () => {
            const n = Number(range.value);
            if (Number.isFinite(n)) this.setKeys(spec, n);
          };
          range.addEventListener("input", commit);
          range.addEventListener("change", commit);
          rangeInput = range;
          readout = out;
          inputs.push(range);
          control.append(range, out);
          break;
        }
        case "pair": {
          const w = el("input", { type: "range", id, min: "3", max: "10", step: "0.1" });
          const h = el("input", { type: "range", min: "2", max: "7.5", step: "0.1" });
          h.setAttribute("aria-label", `${spec.label} height`);
          const wOut = el("span", { class: "readout" });
          const hOut = el("span", { class: "readout" });
          const commit = () => {
            const a = Number(w.value);
            const b = Number(h.value);
            if (Number.isFinite(a) && Number.isFinite(b)) this.setKeys(spec, [a, b]);
          };
          w.addEventListener("input", commit);
          h.addEventListener("input", commit);
          w.addEventListener("change", commit);
          h.addEventListener("change", commit);
          inputs.push(w, h);
          control.append(
            el(
              "div",
              { class: "pair-rows" },
              el("div", { class: "pair-row" }, el("span", { class: "pair-label" }, "Width"), w, wOut),
              el("div", { class: "pair-row" }, el("span", { class: "pair-label" }, "Height"), h, hOut)
            )
          );
          pairOuts = [wOut, hOut];
          break;
        }
        case "enum": {
          const useGlyphs = spec.keys.includes("lines.linestyle") || spec.keys.includes("grid.linestyle");
          if ((spec.options?.length ?? 0) <= 4) {
            const group = el("div", { class: "segmented", id });
            group.setAttribute("role", "radiogroup");
            for (const opt of spec.options ?? []) {
              const glyph = useGlyphs ? LINESTYLE_GLYPHS[opt.value] : void 0;
              const btn = el("button", { type: "button", title: glyph ? opt.label : "" }, glyph ?? opt.label);
              btn.dataset.value = opt.value;
              btn.setAttribute("aria-pressed", "false");
              btn.addEventListener("click", () => this.setKeys(spec, opt.value));
              group.append(btn);
            }
            segmented = group;
            control.append(group);
          } else {
            const select = el("select", { id });
            for (const opt of spec.options ?? []) select.append(el("option", { value: opt.value }, opt.label));
            select.addEventListener("change", () => this.setKeys(spec, select.value));
            inputs.push(select);
            control.append(select);
          }
          break;
        }
        case "colorcycle": {
          const list = el("div", { class: "swatch-list", id });
          for (const preset of spec.presets ?? []) {
            const btn = el("button", { type: "button", class: "preset", title: preset.label });
            btn.dataset.preset = preset.id;
            btn.setAttribute("aria-pressed", "false");
            const sw = el("span", { class: "swatches" });
            for (const c of preset.colors) {
              const i = el("i");
              i.style.background = c;
              i.title = c;
              sw.append(i);
            }
            btn.append(sw);
            btn.addEventListener("click", () => {
              const current = this.effective(spec.keys[0]);
              if (isPropCycle(current) && (current.linewidth || current.linestyle)) {
                const len = preset.colors.length;
                const next = { color: [...preset.colors] };
                if (current.linewidth) next.linewidth = resizeArray(current.linewidth, len, this.allLinesWidth());
                if (current.linestyle) next.linestyle = resizeArray(current.linestyle, len, this.allLinesStyle());
                this.setKeys(spec, next);
              } else {
                this.setKeys(spec, [...preset.colors]);
              }
            });
            list.append(btn);
          }
          swatchList = list;
          control.append(list);
          break;
        }
        case "linecycle": {
          const table = el("div", { class: "linecycle", id });
          const head = el(
            "div",
            { class: "line-row line-head" },
            el("span", {}),
            el("span", {}),
            el("span", {}, "Width"),
            el("span", {}, "Style")
          );
          table.append(head);
          const maxLines = spec.maxLines ?? 8;
          const lineRows = [];
          for (let i = 0; i < maxLines; i++) {
            const idx = el("span", { class: "line-index" }, String(i + 1));
            const color = el("input", { type: "color", class: "line-color" });
            color.setAttribute("aria-label", `Line ${i + 1} color`);
            const width = el("input", { type: "number", class: "line-width", min: "0.25", max: "8", step: "0.25" });
            width.setAttribute("aria-label", `Line ${i + 1} width`);
            const seg = el("div", { class: "segmented line-style" });
            seg.setAttribute("role", "radiogroup");
            seg.setAttribute("aria-label", `Line ${i + 1} style`);
            for (const styleVal of LINESTYLE_VALUES) {
              const btn = el("button", { type: "button", title: LINESTYLE_LABELS[styleVal] }, LINESTYLE_GLYPHS[styleVal]);
              btn.dataset.value = styleVal;
              btn.setAttribute("aria-pressed", "false");
              btn.addEventListener("click", () => {
                for (const b of Array.from(seg.children)) {
                  b.setAttribute("aria-pressed", String(b.dataset.value === styleVal));
                }
                this.commitLineCycle(spec);
              });
              seg.append(btn);
            }
            const commit = () => this.commitLineCycle(spec);
            color.addEventListener("input", commit);
            width.addEventListener("input", commit);
            width.addEventListener("change", commit);
            width.addEventListener("blur", () => this.update());
            const rowEl = el("div", { class: "line-row" }, idx, color, width, seg);
            table.append(rowEl);
            lineRows.push({ row: rowEl, color, width, styleSeg: seg });
          }
          const addBtn = el("button", { type: "button", class: "add-line" }, "+ line");
          addBtn.addEventListener("click", () => this.addLineRow(spec));
          table.append(addBtn);
          control.append(table);
          lineRowsView = lineRows;
          addLineBtnView = addBtn;
          break;
        }
        case "legendloc": {
          const xRange = el("input", { type: "range", id, min: "0", max: "1", step: "0.01" });
          xRange.setAttribute("aria-label", "Legend x");
          const yRange = el("input", { type: "range", min: "0", max: "1", step: "0.01" });
          yRange.setAttribute("aria-label", "Legend y");
          const xOut = el("span", { class: "readout" });
          const yOut = el("span", { class: "readout" });
          const commitXy = () => {
            const x = round(Number(xRange.value));
            const y = round(Number(yRange.value));
            xOut.textContent = x.toFixed(2);
            yOut.textContent = y.toFixed(2);
            if (Number.isFinite(x) && Number.isFinite(y)) this.setKeys(spec, [x, y]);
          };
          xRange.addEventListener("input", commitXy);
          yRange.addEventListener("input", commitXy);
          inputs.push(xRange, yRange);
          const snap = el("select", { class: "snap" });
          snap.append(el("option", { value: "__custom__" }, "Custom"));
          for (const opt of spec.options ?? []) snap.append(el("option", { value: opt.value }, opt.label));
          snap.addEventListener("change", () => {
            if (snap.value !== "__custom__") this.setKeys(spec, snap.value);
          });
          inputs.push(snap);
          const xyRows = el(
            "div",
            { class: "legend-xy" },
            el("div", { class: "legend-xy-row" }, el("span", { class: "legend-label" }, "x"), xRange, xOut),
            el("div", { class: "legend-xy-row" }, el("span", { class: "legend-label" }, "y"), yRange, yOut)
          );
          const snapRow = el("div", { class: "legend-snap" }, el("span", { class: "snap-label" }, "Snap to"), snap);
          legendLoc = { xRange, yRange, xOut, yOut, snap };
          control.append(xyRows, snapRow);
          break;
        }
      }
      if (spec.type !== "linecycle") control.append(revert);
      const view = { spec, row, inputs, badges, revert };
      if (segmented) view.segmented = segmented;
      if (swatchList) view.swatchList = swatchList;
      if (legendLoc) view.legendLoc = legendLoc;
      if (rangeInput) view.rangeInput = rangeInput;
      if (readout) view.readout = readout;
      if (pairOuts) view.pairOuts = pairOuts;
      if (lineRowsView) view.lineRows = lineRowsView;
      if (addLineBtnView) view.addLineBtn = addLineBtnView;
      if (nextRunNote) view.nextRunNote = nextRunNote;
      this.views.set(spec.id, view);
      return row;
    }
    /**
     * Rows shown: the lines in the live figure, any row whose width or style
     * was set explicitly, and rows the user added, within [minLines, maxLines].
     * The color palette's length is deliberately NOT a row count: a ten-color
     * preset does not mean ten lines.
     */
    lineCycleRowCount(spec) {
      const minLines = spec.minLines ?? 2;
      const maxLines = spec.maxLines ?? 8;
      const value = this.settings.rc[spec.keys[0]];
      const figLines = this.figure && this.figure.axes.length ? Math.max(...this.figure.axes.map((a) => a.n_lines)) : 0;
      let explicit = 0;
      if (isPropCycle(value)) {
        const allWidth = this.allLinesWidth();
        const allStyle = this.allLinesStyle();
        value.linewidth?.forEach((w, i) => {
          if (Math.abs(w - allWidth) > 1e-9) explicit = Math.max(explicit, i + 1);
        });
        value.linestyle?.forEach((st, i) => {
          if (st !== allStyle) explicit = Math.max(explicit, i + 1);
        });
      }
      return Math.min(maxLines, Math.max(minLines, figLines, explicit, this.extraLineRows));
    }
    /** Per-row color/width/style for rows [0, rowCount), from the effective prop_cycle plus the all-lines defaults. */
    lineRowValues(spec, rowCount) {
      const value = this.effective(spec.keys[0]);
      const valueColors = isPropCycle(value) ? value.color : Array.isArray(value) ? value : [];
      const widths = isPropCycle(value) ? value.linewidth : void 0;
      const styles = isPropCycle(value) ? value.linestyle : void 0;
      const fallbackColors = CONTROL_FOR_KEY.get("axes.prop_cycle")?.default ?? ["#1f77b4"];
      const palette = valueColors.length ? valueColors : fallbackColors;
      const allWidth = this.allLinesWidth();
      const allStyle = this.allLinesStyle();
      const color = [];
      const width = [];
      const style = [];
      for (let i = 0; i < rowCount; i++) {
        color.push(palette[i % palette.length]);
        width.push(widths?.[i] ?? allWidth);
        style.push(styles?.[i] ?? allStyle);
      }
      return { color, width, style };
    }
    allLinesWidth() {
      const v = this.effective("lines.linewidth");
      return typeof v === "number" ? v : 1.5;
    }
    allLinesStyle() {
      const v = this.effective("lines.linestyle");
      return typeof v === "string" ? v : "-";
    }
    /**
     * Read the visible rows and write the property cycle. The arrays span the
     * whole effective palette (cycler zips equal-length arrays), so colors for
     * lines beyond the table are preserved; those entries carry the all-lines
     * width and style.
     */
    commitLineCycle(spec) {
      const rowCount = this.lineCycleRowCount(spec);
      const rows = this.views.get(spec.id).lineRows;
      const palette = this.lineRowValues(spec, rowCount).color.length ? this.effectivePalette(spec) : [];
      const length = Math.max(rowCount, palette.length);
      const fallbackWidth = this.allLinesWidth();
      const fallbackStyle = this.allLinesStyle();
      const color = [];
      const width = [];
      const style = [];
      for (let i = 0; i < length; i++) {
        if (i < rowCount) {
          const r = rows[i];
          color.push(r.color.value);
          const w = Number(r.width.value);
          width.push(Number.isFinite(w) && w > 0 ? w : fallbackWidth);
          const activeBtn = Array.from(r.styleSeg.children).find(
            (b) => b.getAttribute("aria-pressed") === "true"
          );
          style.push(activeBtn?.dataset.value ?? fallbackStyle);
        } else {
          color.push(palette[i % palette.length]);
          width.push(fallbackWidth);
          style.push(fallbackStyle);
        }
      }
      this.setKeys(spec, this.buildPropCycleValue(color, width, style));
    }
    /** The effective color list for axes.prop_cycle (user value, baseline, or matplotlib's default). */
    effectivePalette(spec) {
      const value = this.effective(spec.keys[0]);
      const colors = isPropCycle(value) ? value.color : Array.isArray(value) ? value : [];
      if (colors.length) return [...colors];
      return [...CONTROL_FOR_KEY.get("axes.prop_cycle")?.default ?? ["#1f77b4"]];
    }
    /** "+ line": reveal one more row (nothing is written until the row is edited). */
    addLineRow(spec) {
      const maxLines = spec.maxLines ?? 8;
      const current = this.lineCycleRowCount(spec);
      if (current >= maxLines) return;
      this.extraLineRows = current + 1;
      this.update();
    }
    /** width/linestyle are included only when at least one row differs from the all-lines default, so the block stays short. */
    buildPropCycleValue(color, width, style) {
      const fallbackWidth = this.allLinesWidth();
      const fallbackStyle = this.allLinesStyle();
      const widthDiffers = width.some((w) => Math.abs(w - fallbackWidth) > 1e-9);
      const styleDiffers = style.some((s) => s !== fallbackStyle);
      if (!widthDiffers && !styleDiffers) return color;
      const value = { color };
      if (widthDiffers) value.linewidth = width;
      if (styleDiffers) value.linestyle = style;
      return value;
    }
    /** Whether `input` is the shadow root's currently-focused element (so update() must not overwrite its value mid-edit). */
    isEditing(input) {
      return this.root.activeElement === input;
    }
    // -------------------------------------------------------------------------
    // DOM: sync with state
    // -------------------------------------------------------------------------
    update() {
      const { ui } = this;
      if (!ui) return;
      const statusText = this.backendState === "none" ? "No live preview (no backend)" : this.backendState === "connecting" ? "Connecting to Python…" : this.backendState === "error" ? `Backend error: ${this.backendMessage}` : `Live preview on · ${this.backendMessage}`;
      ui.pill.title = statusText;
      ui.rail.title = statusText;
      ui.pill.classList.toggle("error", Boolean(this.fenceError));
      ui.rail.classList.toggle("error", Boolean(this.fenceError));
      ui.errMark.hidden = !this.fenceError;
      ui.pill.hidden = this._layoutMode === "rail";
      ui.rail.hidden = this._layoutMode !== "rail";
      ui.pill.classList.toggle("float", this._layoutMode === "float");
      for (const g of GROUPS) {
        const tabView = this.tabViews.get(g.id);
        const visible = this.isGroupVisible(g);
        const hasChanges = this.groupHasChanges(g.id);
        const hasRerun = this.groupHasRerunPending(g.id);
        for (const btn of tabView.buttons) {
          btn.hidden = !visible;
          btn.setAttribute("aria-selected", String(this._category === g.id));
        }
        for (const dot of tabView.dots) dot.hidden = !hasChanges;
        for (const r of tabView.reruns) r.hidden = !hasRerun;
      }
      ui.popover.hidden = !this._open;
      const activeGroup = this._category ? GROUP_BY_ID.get(this._category) : void 0;
      ui.title.textContent = activeGroup ? activeGroup.label : "";
      ui.banner.hidden = !this.fenceError;
      if (this.fenceError) {
        ui.fenceMessage.textContent = `${this.fenceError.message} The panel will not write until this is fixed.`;
      }
      for (const g of GROUPS) {
        const view = ui.groups.get(g.id);
        view.container.hidden = this._category !== g.id || Boolean(this.fenceError);
        const noteNeeded = this.legendNoteNeeded(g);
        view.note.hidden = !noteNeeded;
        if (noteNeeded) view.note.textContent = "The current plot has no legend.";
        if (view.moreRows && view.moreButton) {
          const isOpen = this.moreOpen.get(g.id) ?? false;
          view.moreRows.hidden = !isOpen;
          view.moreButton.textContent = isOpen ? "More ▾" : "More ▸";
        }
      }
      ui.unknownNote.hidden = this.unknownKeys.length === 0;
      if (this.unknownKeys.length) {
        ui.unknownNote.textContent = `The block also sets ${this.unknownKeys.join(", ")}, which this panel has no control for. They are kept as they are.`;
      }
      ui.menu.hidden = !this._menuOpen;
      const activeHasChanges = this._category !== null && this.groupHasChanges(this._category);
      ui.resetCategoryItem.hidden = !activeHasChanges;
      if (activeHasChanges && activeGroup) ui.resetCategoryItem.textContent = `Reset ${activeGroup.label}`;
      ui.resetAllItem.disabled = isDefaultSettings(this.settings) && !this.fenceError;
      ui.showCodeItem.hidden = !this._features.showCode;
      const block = generateBlock(this.settings);
      ui.codePre.textContent = block ?? "# (no block: every setting is at its default)";
      ui.codePre.hidden = !(this._features.showCode && this._codeOpen);
      for (const view of this.views.values()) this.updateControl(view);
    }
    updateControl(view) {
      const { spec, row, badges, revert } = view;
      const isSet = spec.keys.some((k) => k in this.settings.rc);
      const userOverrides = !this.stale && spec.keys.some((k) => this.overridden.has(k));
      const relevantRerunKeys = spec.id === "style" ? ["style"] : spec.keys;
      const rerunPending = (spec.category === "rerun" || !this.canPreview) && relevantRerunKeys.some((k) => this.rerunKeys.has(k));
      row.classList.toggle("is-set", isSet);
      revert.hidden = !isSet;
      badges.replaceChildren();
      if (rerunPending) badges.append(el("span", { class: "badge rerun", title: "Re-run your program to see this change." }, "↻"));
      if (userOverrides) {
        badges.append(el("span", {
          class: "badge user",
          title: "Your code sets this on the current figure; the default above still applies to anything created afterwards."
        }));
      }
      const value = spec.type === "style" ? this.settings.style : this.effective(spec.keys[0] ?? "");
      switch (spec.type) {
        case "style": {
          const select = view.inputs[0];
          const names = this.styles.includes(this.settings.style) ? this.styles : [...this.styles, this.settings.style];
          const current = Array.from(select.options).map((o) => o.value);
          if (current.join("\n") !== names.join("\n")) {
            select.replaceChildren(...names.map((n) => el("option", { value: n }, n === this.settings.style && !this.styles.includes(n) ? `${n} (not available here)` : n)));
          }
          select.value = this.settings.style;
          if (view.nextRunNote) view.nextRunNote.classList.toggle("pending", this.rerunKeys.has("style"));
          break;
        }
        case "copycode":
          break;
        // a button: no value to reflect
        case "bool":
          view.inputs[0].checked = spec.onValue === void 0 ? Boolean(value) : value === spec.onValue;
          break;
        case "number": {
          const v = typeof value === "number" ? String(value) : "";
          const isLineMaster = spec.keys.includes("lines.linewidth");
          const mixed = isLineMaster && this.perLineMixed("linewidth");
          const uniform = isLineMaster ? this.perLineUniform("linewidth") : null;
          const shown = uniform === null ? v : String(uniform);
          row.classList.toggle("mixed", mixed);
          if (view.rangeInput) {
            view.rangeInput.value = shown;
            view.rangeInput.title = mixed ? "Set per line below; drag to make every line the same." : "";
            if (view.readout) view.readout.textContent = mixed ? "mixed" : shown;
          } else if (view.inputs[0] && !this.isEditing(view.inputs[0])) {
            view.inputs[0].value = v;
          }
          break;
        }
        case "fontsize": {
          const input = view.inputs[0];
          const editing = this.isEditing(input);
          const base = this.effective("font.size");
          const baseN = typeof base === "number" ? base : 10;
          let resolved;
          if (typeof value === "number") {
            resolved = value;
            input.title = "";
          } else if (typeof value === "string" && RELATIVE_SIZES.includes(value)) {
            resolved = round(resolveFontSize(value, baseN));
            input.title = `"${value}" = ${resolved} pt at base ${baseN} pt`;
          } else {
            resolved = baseN;
            input.title = "";
          }
          if (!editing) input.value = String(resolved);
          if (view.readout) view.readout.textContent = String(resolved);
          break;
        }
        case "dpi": {
          const input = view.inputs[0];
          const editing = this.isEditing(input);
          let resolved;
          if (typeof value === "number") {
            resolved = value;
            input.title = "";
          } else {
            resolved = this.figure?.dpi ?? 100;
            input.title = "Figure's own dpi (matplotlib default)";
          }
          if (!editing) input.value = String(resolved);
          if (view.readout) view.readout.textContent = String(resolved);
          break;
        }
        case "pair": {
          const [w, h] = view.inputs;
          if (Array.isArray(value) && value.length === 2 && w && h) {
            if (!this.isEditing(w)) w.value = String(value[0]);
            if (!this.isEditing(h)) h.value = String(value[1]);
          }
          if (view.pairOuts) {
            view.pairOuts[0].textContent = w?.value ?? "";
            view.pairOuts[1].textContent = h?.value ?? "";
          }
          break;
        }
        case "enum": {
          const v = value === void 0 ? "" : String(value);
          const mixed = spec.keys.includes("lines.linestyle") && this.perLineMixed("linestyle");
          row.classList.toggle("mixed", mixed);
          if (view.segmented) {
            view.segmented.title = mixed ? "Set per line below; pick one to make every line the same." : "";
            for (const btn of Array.from(view.segmented.children)) {
              btn.setAttribute("aria-pressed", String(!mixed && btn.dataset.value === v));
            }
          } else {
            const select = view.inputs[0];
            if (v && !Array.from(select.options).some((o) => o.value === v)) select.append(el("option", { value: v }, `${v} (from style)`));
            select.value = v;
          }
          break;
        }
        case "colorcycle": {
          const colors = isPropCycle(value) ? value.color : Array.isArray(value) ? value : [];
          const preset = spec.presets?.find((p) => rcEqual(p.colors, colors));
          const list = view.swatchList;
          for (const btn of Array.from(list.querySelectorAll("button.preset[data-preset]"))) {
            btn.setAttribute("aria-pressed", String(btn.dataset.preset === preset?.id));
          }
          list.title = preset ? "" : "Custom colors (from your file)";
          break;
        }
        case "linecycle": {
          const rowCount = this.lineCycleRowCount(spec);
          const maxLines = spec.maxLines ?? 8;
          const { color, width, style } = this.lineRowValues(spec, rowCount);
          const rows = view.lineRows;
          for (let i = 0; i < rows.length; i++) {
            const r = rows[i];
            const visible = i < rowCount;
            r.row.hidden = !visible;
            if (!visible) continue;
            if (!this.isEditing(r.color)) r.color.value = color[i];
            if (!this.isEditing(r.width)) r.width.value = String(width[i]);
            for (const b of Array.from(r.styleSeg.children)) {
              b.setAttribute("aria-pressed", String(b.dataset.value === style[i]));
            }
          }
          if (view.addLineBtn) view.addLineBtn.hidden = rowCount >= maxLines;
          break;
        }
        case "legendloc": {
          const { xRange, yRange, xOut, yOut, snap } = view.legendLoc;
          let x;
          let y;
          if (Array.isArray(value)) {
            snap.value = "__custom__";
            const arr = value;
            x = arr[0] ?? 0;
            y = arr[1] ?? 0;
          } else {
            const name = value === void 0 ? "best" : String(value);
            if (!Array.from(snap.options).some((o) => o.value === name)) {
              snap.append(el("option", { value: name }, `${name} (from style)`));
            }
            snap.value = name;
            [x, y] = this.legendXyForName(name);
          }
          const xs = x.toFixed(2);
          const ys = y.toFixed(2);
          if (!this.isEditing(xRange)) xRange.value = xs;
          if (!this.isEditing(yRange)) yRange.value = ys;
          xOut.textContent = xs;
          yOut.textContent = ys;
          break;
        }
      }
    }
  }
  function round(n) {
    return Math.round(n * 100) / 100;
  }
  function registerPanel(tag = ELEMENT_TAG) {
    if (typeof customElements === "undefined") return;
    if (!customElements.get(tag)) customElements.define(tag, PlotpolishPanel);
  }
  registerPanel();
  class PyodideBackend {
    constructor(pyodide, options = {}) {
      this.pyodide = pyodide;
      this.options = options;
    }
    async runPython(code) {
      this.options.onRun?.(code);
      const isolate = this.options.isolate ?? true;
      let ns = null;
      try {
        let result;
        if (isolate) {
          ns = this.pyodide.toPy({});
          result = this.pyodide.runPython(code, { globals: ns });
        } else {
          result = this.pyodide.runPython(code);
        }
        return toStr(result);
      } finally {
        ns?.destroy?.();
      }
    }
  }
  function toStr(value) {
    if (typeof value === "string") return value;
    if (value && typeof value.toString === "function") {
      const s = String(value);
      value.destroy?.();
      return s;
    }
    return String(value);
  }
  exports.BackendError = BackendError;
  exports.CONTROLS = CONTROLS;
  exports.CONTROL_BY_ID = CONTROL_BY_ID;
  exports.CONTROL_FOR_KEY = CONTROL_FOR_KEY;
  exports.ClipboardSink = ClipboardSink;
  exports.ELEMENT_TAG = ELEMENT_TAG;
  exports.EVENT_PREFIX = EVENT_PREFIX;
  exports.FENCE_END = FENCE_END;
  exports.FENCE_START = FENCE_START;
  exports.FONT_SCALINGS = FONT_SCALINGS;
  exports.FenceError = FenceError;
  exports.GROUPS = GROUPS;
  exports.HELPER_SOURCE = HELPER_SOURCE;
  exports.HelperClient = HelperClient;
  exports.MemorySink = MemorySink;
  exports.PlotpolishPanel = PlotpolishPanel;
  exports.PyLitError = PyLitError;
  exports.PyodideBackend = PyodideBackend;
  exports.RC_KEYS = RC_KEYS;
  exports.RELATIVE_SIZES = RELATIVE_SIZES;
  exports.RESULT_VARIABLE = RESULT_VARIABLE;
  exports.TOOL_NAME = TOOL_NAME;
  exports.VERSION = VERSION;
  exports.buildSnippet = buildSnippet;
  exports.categoryOf = categoryOf;
  exports.controlsInGroup = controlsInGroup;
  exports.defaultSettings = defaultSettings;
  exports.findFence = findFence;
  exports.formatPyValue = formatPyValue;
  exports.generateBlock = generateBlock;
  exports.insertionIndex = insertionIndex;
  exports.isDefaultSettings = isDefaultSettings;
  exports.parseBlock = parseBlock;
  exports.parsePyDict = parsePyDict;
  exports.parsePyString = parsePyString;
  exports.rcEqual = rcEqual;
  exports.registerPanel = registerPanel;
  exports.removeBlock = removeBlock;
  exports.replaceFence = replaceFence;
  exports.resolveFontSize = resolveFontSize;
  exports.upsertBlock = upsertBlock;
  Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
})(this.plotpolish = this.plotpolish || {});
//# sourceMappingURL=plotpolish.iife.js.map
